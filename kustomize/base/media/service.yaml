apiVersion: v1
kind: Service
metadata:
  name: tt-inference-server
spec:
  type: ClusterIP
  selector:
    app: tt-inference-server
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
