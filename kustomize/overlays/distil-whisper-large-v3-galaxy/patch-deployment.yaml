apiVersion: apps/v1
kind: Deployment
metadata:
  name: tt-inference-server
spec:
  template:
    spec:
      containers:
        - name: inference-server
          resources:
            limits:
              memory: 32Gi
            requests:
              memory: 6Gi
          livenessProbe:
            initialDelaySeconds: 600
            periodSeconds: 30
          readinessProbe:
            initialDelaySeconds: 600
            periodSeconds: 10
      volumes:
        - name: cache
          hostPath:
            path: /opt/cache/distil-large-v3-galaxy
