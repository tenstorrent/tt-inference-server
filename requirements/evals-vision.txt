# SPDX-License-Identifier: Apache-2.0
#
# SPDX-FileCopyrightText: © 2025 Tenstorrent USA, Inc.
#
# Dependencies for the EVALS_VISION workflow venv.
# Used by: setup_evals_vision() in workflows/workflow_venvs.py
#
# Uses upstream lmms-eval pinned to v0.4.1 with the vision/multimodal stack.

-c constraints.txt

git+https://github.com/EvolvingLMMs-Lab/lmms-eval.git@v0.4.1
pyjwt
pillow
qwen_vl_utils
# ocrbench_v2 metric deps (TEDS table-tree edit distance + the spotting/vqa
# helpers). lmms-eval's ocrbench_v2 task imports these at module load; without
# them the task errors out and the score reports as N/A.
# Import name -> PyPI package: distance -> Distance.
ipdb
apted
Distance
editdistance
jieba
Levenshtein
