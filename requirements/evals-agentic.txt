# SPDX-License-Identifier: Apache-2.0
#
# SPDX-FileCopyrightText: © 2026 Tenstorrent USA, Inc.
#
# Dependencies for the EVALS_AGENTIC workflow venv.
# Used by: setup_evals_agentic() in workflows/workflow_venvs.py
#
# Harbor is NOT listed here — it is cloned from GitHub and installed as an
# editable package by the setup_evals_agentic() hook, so that its top-level
# adapters/ directory (e.g. tau3-bench, which is not shipped in the PyPI wheel)
# is importable by agent_import_path.
#
# Agent packages are deliberately absent. Harbor installs the agent inside the
# task container (mini-swe-agent via `uv tool install mini-swe-agent==<version>`
# from the `version` agent kwarg), so a host-side copy would be unused. The
# SWE-bench grading harness likewise runs in-container, from the pinned
# dependencies in each task's tests/test.sh — nothing here.

orjson>=3.11.6,<4.0
backoff>=2.0

# Mints the JWT bearer token the vLLM/tt-metal server authorizes against
# (test_module.llm_tests.agentic_eval_tests._configure_openai_env ->
# utils.auth_helpers.setup_tests_auth, which imports jwt unconditionally).
# Pinned via constraints.txt (pyjwt==2.7.0).
pyjwt
