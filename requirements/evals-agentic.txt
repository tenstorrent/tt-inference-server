# SPDX-License-Identifier: Apache-2.0
#
# SPDX-FileCopyrightText: © 2026 Tenstorrent USA, Inc.
#
# Dependencies for the EVALS_AGENTIC workflow venv.
# Used by: setup_evals_agentic() in workflows/workflow_venvs.py
#
# SWE-agent and Harbor are NOT listed here — they are cloned from GitHub and
# installed as editable packages by the setup_evals_agentic() hook. SWE-agent
# needs its config directory on disk; Harbor is cloned (rather than installed
# from PyPI) so that its top-level adapters/ directory (e.g. tau3-bench, which
# is not shipped in the PyPI wheel) is importable by agent_import_path.

mini-swe-agent==2.2.8
git+https://github.com/epoch-research/SWE-bench.git@bc2a82af2874e26fa6f206ae0ad9017c4768daa2

orjson>=3.11.6,<4.0
backoff>=2.0

# Mints the JWT bearer token the vLLM/tt-metal server authorizes against
# (test_module.llm_tests.agentic_eval_tests._configure_openai_env ->
# utils.auth_helpers.setup_tests_auth, which imports jwt unconditionally).
# Pinned via constraints.txt (pyjwt==2.7.0).
pyjwt