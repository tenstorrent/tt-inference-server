--extra-index-url https://pypi.eng.aws.tenstorrent.com
--extra-index-url https://download.pytorch.org/whl/cpu

# pjrt-plugin-tt is installed separately in Dockerfile.forge from the Tenstorrent PyPI.
# It pulls torch and torch-xla as transitive dependencies.

torchvision

tabulate
timm # input preprocessing
flax # input preprocessing
pytorchcv # for vovnet

datasets # model training
peft # model training
