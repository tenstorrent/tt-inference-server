--extra-index-url https://pypi.eng.aws.tenstorrent.com
--extra-index-url https://download.pytorch.org/whl/cpu
# 1.4.0 dev/nightly built 2026-07-18. Supersedes the 2026-07-05 build.
tt-forge==1.4.0

tabulate
timm # input preprocessing
flax # input preprocessing
pytorchcv # for vovnet
tabulate

diffusers==0.37.1
transformers

mistral-common[image]>=1.11.7 # mistral's tokenizer

datasets # model training
# Pinned so the adapter_config.json schema this writes is deterministic.
peft==0.20.0 # model training

# yolox requirements
thop
pycocotools>=2.0.2
