--extra-index-url https://pypi.eng.aws.tenstorrent.com
--extra-index-url https://download.pytorch.org/whl/cpu
# 1.4.0 dev/nightly built 2026-07-18. Supersedes the 2026-07-05 build.
tt-forge==1.4.0

tabulate
timm # input preprocessing
flax # input preprocessing
pytorchcv # for vovnet
tabulate

diffusers==0.37.1
transformers

mistral-common[image]==1.11.7 # mistral's tokenizer

datasets # model training
peft # model training

# Training recipes reused by trainer runners. Pinned to a commit rather
# than a branch so a worker image build is reproducible.
blacksmith @ git+https://github.com/tenstorrent/tt-blacksmith.git@b5a35e6af09b3aba488e0adfd4a8b0cc8d6f826f
# blacksmith declares no install_requires, so its transitive imports are listed here.
pandas
torchvision
tqdm
wandb

# yolox requirements
thop
pycocotools>=2.0.2
