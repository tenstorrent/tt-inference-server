--extra-index-url https://pypi.eng.aws.tenstorrent.com
--extra-index-url https://download.pytorch.org/whl/cpu
# 1.4.0 dev/nightly built 2026-07-18. Supersedes the 2026-07-05 build.
tt-forge==1.4.0

tabulate
timm # input preprocessing
flax # input preprocessing
pytorchcv # for vovnet
tabulate

diffusers==0.37.1
transformers

mistral-common[image]==1.11.7 # mistral's tokenizer

datasets # model training
peft # model training

# Training recipes reused by trainer_training_lora_runner. Pinned to a commit rather
# than a branch so a worker image build is reproducible.
blacksmith @ git+https://github.com/tenstorrent/tt-blacksmith.git@5ca53306de7a4f382d630c387876d118bc69f22f
# blacksmith declares no install_requires, so its transitive imports are listed
# here. jax is only reached through ReproducibilityManager, which this runner
# does not use, but blacksmith imports it eagerly.
# TODO: REMOVE jax
jax
pandas
torchvision
tqdm
wandb

# yolox requirements
thop
pycocotools>=2.0.2
