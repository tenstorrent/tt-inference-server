# Build output and CMake / FetchContent (build/, build-tracy/, build_tsan/, …)
build*/
!.cursor/
!.cursor/**
tsan_build/
.cache/

# Object and dependency files (in-source or stray)
*.o
*.o.d
*.obj
*.obj.d
*.tracy

# In-source CMake artifacts (if cmake was run from cpp_server/ by mistake)
CMakeFiles/

tt-llm-engine/

# Runtime logs (Drogon setLogPath)
logs/

**/vllm-venv

deps/

# Profiling output (flamegraph-capture[-offcpu].sh writes here)
bench_results/

# CI integration test artifacts (cpp_server/tests/ci/_artifacts/)
tests/ci/_artifacts/

# Override root-level *.json ignore
!resources/openapi.json
!.mcp.json
