// Abstract interface for remote KV manager

#pragma once

#include <string>
#include <vector>

namespace tt::domain::llm {

class RemoteKvManager {
  public:
    virtual ~RemoteKvManager() = default;

    
}