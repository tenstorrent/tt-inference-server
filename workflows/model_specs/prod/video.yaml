# Model spec catalog: Video templates.
# Do not edit manually. This file is updated through the automated release process.
# Schema: workflows/model_spec.py (ModelSpecTemplate, DeviceModelSpec).
templates:
# =============================================================================
# Video templates
# =============================================================================
- weights:
    - genmo/mochi-1-preview
  version: "0.10.0"
  tt_metal_commit: 555f240
  impl: tt_transformers
  min_disk_gb: 60
  min_ram_gb: 32
  model_type: VIDEO
  inference_engine: MEDIA
  device_model_specs:
    - device: T3K
      max_concurrency: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
    - device: GALAXY
      max_concurrency: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
    - device: P150X4
      max_concurrency: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 30000000
    - device: P150X8
      max_concurrency: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 30000000
    - device: P300X2
      max_concurrency: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 30000000
  status: COMPLETE

- weights:
    - Wan-AI/Wan2.2-T2V-A14B-Diffusers
  version: "0.10.0"
  tt_metal_commit: 555f240
  impl: tt_transformers
  min_disk_gb: 60
  min_ram_gb: 32
  model_type: VIDEO
  inference_engine: MEDIA
  device_model_specs:
    - device: T3K
      max_concurrency: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
    - device: GALAXY
      max_concurrency: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
    - device: P150X4
      max_concurrency: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 30000000
  status: COMPLETE

- weights:
    - Wan-AI/Wan2.2-T2V-A14B-Diffusers
  version: "0.10.1"
  tt_metal_commit: 555f240
  impl: tt_transformers
  min_disk_gb: 60
  min_ram_gb: 32
  model_type: VIDEO
  inference_engine: MEDIA
  device_model_specs:
    - device: P150X8
      max_concurrency: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 30000000
  status: COMPLETE

- weights:
    - Wan-AI/Wan2.2-T2V-A14B-Diffusers
  version: "0.17.0"
  tt_metal_commit: "8c48a10"
  impl: tt_transformers
  min_disk_gb: 60
  min_ram_gb: 32
  model_type: VIDEO
  inference_engine: MEDIA
  env_vars:
    TT_DIT_CACHE_DIR: /home/container_app_user/cache_root/tt_dit_cache
  device_model_specs:
    - device: P300X2
      max_concurrency: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 30000000
  status: COMPLETE

- weights:
    - Lightricks/LTX-2.3-distilled  # basename -> MODEL=LTX-2.3-distilled (must match tt-media-server ModelNames)
  hf_weights_repo: Lightricks/LTX-2.3  # real gated repo the weights download from
  version: "0.19.0"  # matches VERSION file; media image tag = 0.19.0-88183a0c4658
  tt_metal_commit: 88183a0c46589e87cce14adf0441de6bd562ca9a  # tt-metal main HEAD baked into the tt-shield build
  impl: tt_transformers
  min_disk_gb: 80  # weights ~69 GB
  min_ram_gb: 32
  model_type: VIDEO
  inference_engine: MEDIA
  env_vars:
    TT_DIT_CACHE_DIR: /home/container_app_user/cache_root/tt_dit_cache
    TT_VIDEO_OUTPUT_DIR: /home/container_app_user/cache_root/tt-media-videos
    LTX_YUV_EXPORT: "1"
  device_model_specs:
    - device: GALAXY
      max_concurrency: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
  status: COMPLETE
