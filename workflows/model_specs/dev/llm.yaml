# Model spec catalog: LLM templates.
# Edit this file to add or modify a LLM model spec.
# Schema: workflows/model_spec.py (ModelSpecTemplate, DeviceModelSpec).
templates:
# =============================================================================
# LLM templates
# =============================================================================
- weights:
    - openai/gpt-oss-20b
  impl: gpt_oss
  inference_engine: VLLM
  device_model_specs:
    - device: T3K
      max_concurrency: 1
      max_context: 16384  # 16 * 1024
      default_impl: true
      env_vars:
        VLLM_ENABLE_RESPONSES_API_STORE: 1
        VLLM_GPT_OSS_HARMONY_SYSTEM_INSTRUCTIONS: 1
    - device: GALAXY_T3K
      max_concurrency: 1
      max_context: 16384  # 16 * 1024
      default_impl: true
      env_vars:
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/t3k_mesh_graph_descriptor.textproto"
    - device: GALAXY
      max_concurrency: 128  # 32 * 4
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        MESH_DEVICE: "(4, 8)"  # Override default TG->(8,4) to use (4,8) mesh grid
      vllm_args:
        data_parallel_size: 4
  status: EXPERIMENTAL
  has_builtin_warmup: true
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    openai/gpt-oss-20b:
      reasoning_parser_name: openai_gptoss
      tool_call_parser_name: openai

- weights:
    - openai/gpt-oss-120b
  impl: gpt_oss
  inference_engine: VLLM
  device_model_specs:
    - device: T3K
      max_concurrency: 1
      max_context: 16384  # 16 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      override_tt_config:
        trace_region_size: 134217728  # 128 MB; vLLM default 50 MB is too small for long prefill
    - device: GALAXY
      max_concurrency: 32  # currently limiting client-side max_concurrency=32 to allow workflows to
      # complete else they will timeout due to hitting the vLLM RPC recv 30min timeout
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      env_vars:
        MESH_DEVICE: "(4, 8)"  # Override default TG->(8,4) to use (4,8) mesh grid
        TT_MM_THROTTLE_PERF: 2
      vllm_args:
        data_parallel_size: 4
        max_num_seqs: 32  # override the default inferred by max_concurrency
      override_tt_config:
        sample_on_device_mode: all
        trace_region_size: 134217728  # 128 MB; vLLM default 50 MB is too small for long prefill
      known_issues:
        - workflow_type: EVALS
          task_name: aime25
          reason: "Model implementation is producing non-deterministic outputs causing this eval task to pass/fail flakily. Masking this part of the acceptance criteria due to other evals passing consistently."
        - workflow_type: EVALS
          task_name: gpqa_diamond_cot_zeroshot
          reason: "Model implementation is producing non-deterministic outputs causing this eval task to pass/fail flakily. Masking this part of the acceptance criteria due to other evals passing consistently."
  status: EXPERIMENTAL
  has_builtin_warmup: true
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    openai/gpt-oss-120b:
      reasoning_parser_name: openai_gptoss
      tool_call_parser_name: openai

- weights:
    - openai/gpt-oss-120b
  impl: gpt_oss
  inference_engine: VLLM
  device_model_specs:
    - device: P300X2
      max_concurrency: 1
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      env_vars:
        MESH_DEVICE: "(1, 4)"
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/p300_x2_mesh_graph_descriptor.textproto"
      override_tt_config:
        trace_region_size: 58000000
        sample_on_device_mode: decode_only
      known_issues:
        - workflow_type: EVALS
          task_name: aime25
          reason: "Model implementation is producing non-deterministic outputs causing this eval task to pass/fail flakily. Masking this part of the acceptance criteria due to other evals passing consistently."
        - workflow_type: EVALS
          task_name: gpqa_diamond_cot_zeroshot
          reason: "Model implementation is producing non-deterministic outputs causing this eval task to pass/fail flakily. Masking this part of the acceptance criteria due to other evals passing consistently."
  status: EXPERIMENTAL
  has_builtin_warmup: true
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    openai/gpt-oss-120b:
      reasoning_parser_name: openai_gptoss
      tool_call_parser_name: openai

- weights:
    - arcee-ai/AFM-4.5B
  impl: tt_transformers
  inference_engine: VLLM
  # need to add default sampling params here because they're
  # not in generation_config.json
  # see: https://github.com/tenstorrent/tt-inference-server/issues/1066
  device_model_specs:
    - device: N300
      max_concurrency: 32
      max_context: 65536  # 64 * 1024
      default_impl: true
      vllm_args:
        override_generation_config: '{"temperature": 0.5, "top_k": 50, "top_p": 0.95}'
    - device: T3K
      max_concurrency: 32
      max_context: 65536  # 64 * 1024
      default_impl: true
      vllm_args:
        override_generation_config: '{"temperature": 0.5, "top_k": 50, "top_p": 0.95}'
  status: EXPERIMENTAL

- weights:
    - google/gemma-3-1b-it
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: N150
      max_concurrency: 32
      max_context: 32768  # 32 * 1024
      default_impl: true
      override_tt_config:
        l1_small_size: 4096
        fabric_config: FABRIC_1D
  status: EXPERIMENTAL
  has_builtin_warmup: true

- weights:
    - google/gemma-4-31B-it
  impl: tt_transformers
  # Dev specs omit tt_metal_commit/vllm_commit (base ModelSpecTemplate rejects
  # them; only prod specs carry commits). The image is supplied at runtime via
  # --override-docker-image; the prod gemma4 specs pin the build commits.
  inference_engine: VLLM
  device_model_specs:
    - device: P300X2  # QB2 (2x P300, 4-chip TP mesh)
      max_concurrency: 1
      # Hybrid-off DRAM ceiling on QB2: with hybrid KV groups disabled every one
      # of the 60 layers allocates a full-length KV buffer, so the largest pool
      # that fits in DRAM (and leaves prefill scratch headroom) is ~49K, not 131K.
      # Drives max_model_len + max_num_batched_tokens; the matching KV-pool size is
      # set via GEMMA4_MAX_TOKENS_ALL_USERS below. Servable prompt tops out at the
      # 32768 power-of-2 prefill bucket (next bucket, 65536, exceeds this pool).
      max_context: 49152  # 48 * 1024
      default_impl: true
      env_vars:
        # QB2's 4 Blackhole chips are driven as a P150x4 (1,4) mesh — matches the
        # passing vLLM nightly [BH-QB-2] Gemma4 entry. The custom p300_x2 descriptor
        # laid the TP collectives over the wrong fabric links and corrupted decode
        # logits; the default p150_x4 descriptor (selected from MESH_DEVICE) is correct.
        MESH_DEVICE: "P150x4"
        GEMMA4_PAGE_BLOCK_SIZE: "64"
        # Size the hybrid-off all-user KV pool to fit QB2 DRAM. The container
        # sizes the pool from Gemma4ForCausalLM.get_max_tokens_all_users, which
        # honors this env override (value kept here, gated by device + model,
        # rather than hardcoded in tt-metal).
        GEMMA4_MAX_TOKENS_ALL_USERS: "49152"
      override_tt_config:
        fabric_config: FABRIC_1D
        trace_region_size: 200000000
        # On-device decode sampling is required on the TP mesh so token ids
        # >= 65536 are reachable (host sampling only sees device 0's vocab shard).
        sample_on_device_mode: decode_only
      vllm_args:
        # Sampling params (temp=1.0 / top_k=64 / top_p=0.95) load from the
        # model's HF generation_config.json; evals override per-request. No
        # override_generation_config here -- it would just restate the HF default.
        # Activate the gemma4 tool_call_parser so tool_choice:"auto" clients
        # (mini-swe-agent / SWE-Bench) work on a flag-free release run. vLLM
        # requires --tool-call-parser alongside --enable-auto-tool-choice, so it
        # is passed explicitly here; the gemma4 parser is registered by the
        # vllm-tt-plugin entry point (tenstorrent/vllm #431).
        enable-auto-tool-choice: true
        tool-call-parser: gemma4
        # Enable gemma-4 thinking server-side so the r1_gpqa_diamond /
        # terminal-bench-2 / swe-bench-verified evals (methodology matched to
        # tenstorrent/tt-inference-server#4331) get the reasoning channel the
        # chat template otherwise suppresses (gemma-4 scores ~10pts lower w/o it).
        # --reasoning-parser separates the thought channel into reasoning_content.
        default-chat-template-kwargs: '{"enable_thinking": true}'
        reasoning-parser: gemma4
    # GPU (H100 NVL) bring-your-own vLLM server (see docs/gpu_workflows.md); used
    # to collect GPU reference eval scores for GPQA-Diamond, Terminal-Bench 2.0,
    # and SWE-Bench Verified. Dev-only; no tt_metal_commit/version pin needed
    # (the bring-your-own-server path builds no tt-metal docker image).
    - device: GPU
      max_concurrency: 32
      max_context: 204800  # 200 * 1024; native ctx is 256K but a single H100 NVL (94GB, bf16 KV) caps servable ctx at ~205K (KV cache = 210,605 tokens)
      default_impl: true
  status: EXPERIMENTAL
  has_builtin_warmup: true
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    google/gemma-4-31B-it:
      reasoning_parser_name: gemma4
      # tool parser ships in vllm-tt-plugin as of tenstorrent/vllm #431; activated
      # by enable-auto-tool-choice in vllm_args above.
      tool_call_parser_name: gemma4

- weights:
    - google/gemma-4-12B-it
  impl: tt_transformers
  # Dev specs omit tt_metal_commit/vllm_commit (base ModelSpecTemplate rejects
  # them; only prod specs carry commits). The image is supplied at runtime via
  # --override-docker-image; the prod gemma4 specs pin the build commits.
  inference_engine: VLLM
  device_model_specs:
    - device: P300X2  # QB2 (2x P300, 4-chip TP mesh)
      max_concurrency: 1
      # 12B (unlike 31B) is NOT pool-capped: hybrid-off, its ~131K all-user KV
      # pool fits QB2 DRAM, so no GEMMA4_MAX_TOKENS_ALL_USERS override is set and
      # the pool == the largest prefill bucket (131072). That means the
      # page-table "negative dimension" fault (which bites 31B because its 49152
      # pool is smaller than the 65536 bucket) cannot occur here — every prefill
      # bucket <= 131072 fits. The pool must also stay this large to leave block
      # headroom for decode above a 64K-input (65536-bucket) prompt. The only
      # real limit is the >64K eager-prefill fabric hang (tt-metal#48289), which
      # is guarded at the eval layer (max_input_tokens = 64K, pads to the 65536
      # bucket — the largest validated one). Hence max_context stays 131072
      # rather than a hard 65536 cap, which would zero out decode room at 64K in.
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        # QB2's 4 Blackhole chips are driven as a P150x4 (1,4) mesh — matches the
        # passing vLLM nightly [BH-QB-2] Gemma4 entry. The custom p300_x2 descriptor
        # laid the TP collectives over the wrong fabric links and corrupted decode
        # logits; the default p150_x4 descriptor (selected from MESH_DEVICE) is correct.
        MESH_DEVICE: "P150x4"
        GEMMA4_PAGE_BLOCK_SIZE: "64"
      override_tt_config:
        fabric_config: FABRIC_1D
        trace_region_size: 100000000  # match vLLM nightly QB2 Gemma4-12B entry
        sample_on_device_mode: decode_only
      vllm_args:
        # Sampling params (temp=1.0 / top_k=64 / top_p=0.95) load from the
        # model's HF generation_config.json; evals override per-request. No
        # override_generation_config here -- it would just restate the HF default.
        # Activate the gemma4 tool_call_parser so tool_choice:"auto" clients
        # (mini-swe-agent / SWE-Bench) work on a flag-free release run. vLLM
        # requires --tool-call-parser alongside --enable-auto-tool-choice, so it
        # is passed explicitly here; the gemma4 parser is registered by the
        # vllm-tt-plugin entry point (tenstorrent/vllm #431).
        enable-auto-tool-choice: true
        tool-call-parser: gemma4
        # Enable gemma-4 thinking server-side so the r1_gpqa_diamond /
        # terminal-bench-2 / swe-bench-verified evals (methodology matched to
        # tenstorrent/tt-inference-server#4331) get the reasoning channel the
        # chat template otherwise suppresses (gemma-4 scores ~10pts lower w/o it).
        # --reasoning-parser separates the thought channel into reasoning_content.
        default-chat-template-kwargs: '{"enable_thinking": true}'
        reasoning-parser: gemma4
    # GPU (H100 NVL) bring-your-own vLLM server (see docs/gpu_workflows.md); used
    # to collect GPU reference eval scores for GPQA-Diamond, Terminal-Bench 2.0,
    # and SWE-Bench Verified. Dev-only; no tt_metal_commit/version pin needed
    # (the bring-your-own-server path builds no tt-metal docker image).
    - device: GPU
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
  status: EXPERIMENTAL
  has_builtin_warmup: true
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    google/gemma-4-12B-it:
      reasoning_parser_name: gemma4
      # tool parser ships in vllm-tt-plugin as of tenstorrent/vllm #431; activated
      # by enable-auto-tool-choice in vllm_args above.
      tool_call_parser_name: gemma4

- weights:
    - Qwen/Qwen3-8B
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: N150
      max_concurrency: 32
      max_context: 32768  # 32 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 51000000
    - device: N300
      max_concurrency: 32
      max_context: 40960
      default_impl: true
      override_tt_config:
        trace_region_size: 65000000
    - device: T3K
      max_concurrency: 32
      max_context: 40960
      default_impl: true
    - device: GALAXY_T3K
      max_concurrency: 32
      max_context: 40960
      default_impl: true
      env_vars:
        TT_MM_THROTTLE_PERF: 5
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/t3k_mesh_graph_descriptor.textproto"
      override_tt_config:
        fabric_config: FABRIC_1D
    - device: GALAXY
      max_concurrency: 128  # 32 * 4
      max_context: 40960
      default_impl: true
      vllm_args:
        data_parallel_size: 4
      env_vars:
        TT_MM_THROTTLE_PERF: 5
  status: FUNCTIONAL
  metadata:
    Qwen/Qwen3-8B:
      reasoning_parser_name: qwen3
      tool_call_parser_name: hermes

- weights:
    - Qwen/Qwen3-8B
  impl: tt_transformers
  system_requirements:
    firmware:
      specifier: ">=19.2.0"
      mode: STRICT
    kmd:
      specifier: ">=2.5.0"
      mode: STRICT
  inference_engine: VLLM
  device_model_specs:
    - device: P300
      max_concurrency: 32
      max_context: 40960
      default_impl: true
      env_vars:
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/p300_mesh_graph_descriptor.textproto"
  status: FUNCTIONAL
  metadata:
    Qwen/Qwen3-8B:
      reasoning_parser_name: qwen3
      tool_call_parser_name: hermes

- weights:
    - Qwen/Qwen3.6-27B
  impl: qwen36_blackhole
  inference_engine: VLLM
  model_type: LLM
  supported_modalities:
    - text
  device_model_specs:
    - device: P300X2
      max_concurrency: 1
      max_context: 262144
      default_impl: true
      env_vars:
        TT_QWEN35_TEXT_VER: qwen36_blackhole
        HF_MODEL: Qwen/Qwen3.6-27B
        MESH_DEVICE: "(1, 4)"
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/p300_x2_mesh_graph_descriptor.textproto"
      vllm_args:
        async_scheduling: true
        enable_auto_tool_choice: true
        reasoning_parser: qwen3
        tool_call_parser: qwen3_coder
      override_tt_config:
        fabric_config: FABRIC_1D
        # Magic number; follows vllm nightly:
        # https://github.com/tenstorrent/tt-metal/actions/runs/28493777891/job/84457052459#step:10:1
        trace_region_size: 268435456
        sample_on_device_mode: decode_only
  # EXPERIMENTAL: no target performance has been set yet, so we can't classify
  # this as FUNCTIONAL, COMPLETE, or top-perf.
  status: EXPERIMENTAL
  has_builtin_warmup: true
  metadata:
    Qwen/Qwen3.6-27B:
      reasoning_parser_name: qwen3
      tool_call_parser_name: qwen3_coder

- weights:
    - Qwen/Qwen3-32B
  impl: qwen3_32b_galaxy
  inference_engine: VLLM
  device_model_specs:
    - device: GALAXY
      max_concurrency: 32  # 8 * 4
      # NOTE: model natively supports 40K but use this to override max_num_batched_tokens
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
      vllm_args:
        data_parallel_size: 4
      override_tt_config:
        dispatch_core_axis: col
        sample_on_device_mode: all
        fabric_config: FABRIC_1D_RING
        worker_l1_size: 1344544
        trace_region_size: 184915840
    - device: BLACKHOLE_GALAXY
      max_concurrency: 32  # 8 * 4
      # NOTE: model natively supports 40K but use this to override max_num_batched_tokens
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
      vllm_args:
        data_parallel_size: 4
      override_tt_config:
        dispatch_core_axis: col
        sample_on_device_mode: all
        fabric_config: FABRIC_1D_RING
        worker_l1_size: 1344544
        trace_region_size: 184915840
  system_requirements:
    firmware:
      specifier: ">=18.6.0"
      mode: STRICT
    kmd:
      specifier: ">=2.1.0"
      mode: STRICT
  status: COMPLETE
  has_builtin_warmup: true
  metadata:
    Qwen/Qwen3-32B:
      reasoning_parser_name: qwen3
      tool_call_parser_name: hermes

- weights:
    - Qwen/Qwen3-32B
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 54000000
    - device: GALAXY_T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        TT_MM_THROTTLE_PERF: 5
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/t3k_mesh_graph_descriptor.textproto"
      override_tt_config:
        fabric_config: FABRIC_1D
    - device: GALAXY
      max_concurrency: 128  # 32 * 4
      max_context: 131072  # 128 * 1024
      default_impl: false
      override_tt_config:
        trace_region_size: 66147328
        sample_on_device_mode: decode_only
      vllm_args:
        data_parallel_size: 4
      env_vars:
        TT_MM_THROTTLE_PERF: 5
    - device: BLACKHOLE_GALAXY
      max_concurrency: 128  # 32 * 4
      max_context: 131072  # 128 * 1024
      default_impl: false
      override_tt_config:
        trace_region_size: 66147328
        sample_on_device_mode: decode_only
      vllm_args:
        data_parallel_size: 4
      env_vars:
        TT_MM_THROTTLE_PERF: 5
  status: FUNCTIONAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    Qwen/Qwen3-32B:
      reasoning_parser_name: qwen3
      tool_call_parser_name: hermes

- weights:
    - Qwen/Qwen3-32B
  impl: tt_transformers
  system_requirements:
    firmware:
      specifier: ">=18.12.0"
      mode: STRICT
    kmd:
      specifier: ">=2.4.1"
      mode: STRICT
  inference_engine: VLLM
  device_model_specs:
    - device: P150X8
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 61341696
  status: FUNCTIONAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    Qwen/Qwen3-32B:
      reasoning_parser_name: qwen3
      tool_call_parser_name: hermes

- weights:
    - Qwen/Qwen3-32B
  impl: tt_transformers
  system_requirements:
    firmware:
      specifier: ">=19.2.0"
      mode: STRICT
    kmd:
      specifier: ">=2.5.0"
      mode: STRICT
  inference_engine: VLLM
  device_model_specs:
    - device: P300X2
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 402653184  # 384 * 1024 * 1024
  status: FUNCTIONAL
  has_builtin_warmup: true
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    Qwen/Qwen3-32B:
      reasoning_parser_name: qwen3
      tool_call_parser_name: hermes

- weights:
    - mistralai/Mistral-7B-Instruct-v0.3
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: N150
      max_concurrency: 32
      max_context: 32768  # 32 * 1024
      default_impl: true
    - device: N300
      max_concurrency: 32
      max_context: 32768  # 32 * 1024
      default_impl: true
    - device: T3K
      max_concurrency: 32
      max_context: 32768  # 32 * 1024
      default_impl: true
  status: COMPLETE
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1

- weights:
    - mistralai/Mistral-Small-3.1-24B-Instruct-2503
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 90000000
      vllm_args:
        limit-mm-per-prompt: '{"image": 1}'
        mm-processor-cache-gb: 0
  model_type: VLM
  status: EXPERIMENTAL
  has_builtin_warmup: true
  supported_modalities:
    - text
    - image
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1

- weights:
    - Qwen/QwQ-32B
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
    - device: GALAXY
      max_concurrency: 128  # 32 * 4
      max_context: 131072  # 128 * 1024
      default_impl: true
      vllm_args:
        data_parallel_size: 4
      override_tt_config:
        trace_region_size: 27381760
      env_vars:
        TT_MM_THROTTLE_PERF: 5
    - device: GALAXY_T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        TT_MM_THROTTLE_PERF: 5
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/t3k_mesh_graph_descriptor.textproto"
      override_tt_config:
        fabric_config: FABRIC_1D
  status: FUNCTIONAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    Qwen/QwQ-32B:
      reasoning_parser_name: deepseek_r1
      tool_call_parser_name: hermes

- weights:
    - Qwen/Qwen2.5-72B
    - Qwen/Qwen2.5-72B-Instruct
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      override_tt_config:
        trace_region_size: 66000000
    - device: GALAXY
      max_concurrency: 128  # 32 * 4
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      vllm_args:
        data_parallel_size: 4
      override_tt_config:
        trace_region_size: 66000000
      env_vars:
        TT_MM_THROTTLE_PERF: 5
    - device: GALAXY_T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      override_tt_config:
        trace_region_size: 40000000
        fabric_config: FABRIC_1D
      env_vars:
        TT_MM_THROTTLE_PERF: 5
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/t3k_mesh_graph_descriptor.textproto"
  status: FUNCTIONAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
    MAX_PREFILL_CHUNK_SIZE: "16"
  metadata:
    Qwen/Qwen2.5-72B:
      tool_call_parser_name: hermes
    Qwen/Qwen2.5-72B-Instruct:
      tool_call_parser_name: hermes

- weights:
    - Qwen/Qwen2.5-7B
    - Qwen/Qwen2.5-7B-Instruct
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: N300
      max_concurrency: 16
      max_context: 32768  # 32 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 60000000
    - device: N150X4
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
  status: EXPERIMENTAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    Qwen/Qwen2.5-7B:
      tool_call_parser_name: hermes
    Qwen/Qwen2.5-7B-Instruct:
      tool_call_parser_name: hermes

- weights:
    - meta-llama/Llama-3.3-70B-Instruct
    - meta-llama/Llama-3.1-70B
    - meta-llama/Llama-3.1-70B-Instruct
    - deepseek-ai/DeepSeek-R1-Distill-Llama-70B
  impl: llama3_70b_galaxy
  inference_engine: VLLM
  device_model_specs:
    - device: GALAXY
      max_concurrency: 32  # 8 * 4
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      vllm_args:
        data_parallel_size: 4
      override_tt_config:
        dispatch_core_axis: col
        sample_on_device_mode: all
        fabric_config: FABRIC_1D_RING
        worker_l1_size: 1344544
        trace_region_size: 215000000
    - device: BLACKHOLE_GALAXY
      max_concurrency: 32  # 8 * 4
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      vllm_args:
        data_parallel_size: 4
      override_tt_config:
        dispatch_core_axis: col
        sample_on_device_mode: all
        fabric_config: FABRIC_1D_RING
        worker_l1_size: 1344544
        trace_region_size: 215000000
  system_requirements:
    firmware:
      specifier: ">=18.6.0"
      mode: STRICT
    kmd:
      specifier: ">=2.1.0"
      mode: STRICT
  status: COMPLETE
  has_builtin_warmup: true
  metadata:
    meta-llama/Llama-3.3-70B-Instruct:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-70B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-70B-Instruct:
      tool_call_parser_name: llama3_json
    deepseek-ai/DeepSeek-R1-Distill-Llama-70B:
      reasoning_parser_name: deepseek_r1
      tool_call_parser_name: deepseek_v3

- weights:
    - deepseek-ai/DeepSeek-R1-0528
  impl: deepseek_r1_galaxy
  inference_engine: VLLM
  device_model_specs:
    - device: GALAXY
      max_concurrency: 256  # 32 * 8
      max_context: 2048
      default_impl: true
      tensor_cache_timeout: 6400.0
      vllm_args:
        max_model_len: "2048"
    - device: DUAL_GALAXY
      max_concurrency: 256  # 32 * 8; 32 per DP rank * 8 ranks
      max_context: 32768
      default_impl: true
      tensor_cache_timeout: 6400.0
      vllm_args:
        data_parallel_size: 8
        block_size: "32"
      override_tt_config:
        fabric_config: FABRIC_1D
        fabric_reliability_mode: RELAXED_INIT
        trace_mode: none
        env_passthrough:
          - DEEPSEEK_V3_CACHE
          - DEEPSEEK_V3_HF_MODEL
    - device: QUAD_GALAXY
      max_concurrency: 512  # 32 * 16; 32 per DP rank * 16 ranks
      max_context: 32768
      default_impl: true
      vllm_args:
        data_parallel_size: 16
        block_size: "32"
        max_model_len: "2048"
      override_tt_config:
        fabric_config: FABRIC_1D
        fabric_reliability_mode: RELAXED_INIT
        trace_mode: none
        env_passthrough:
          - DEEPSEEK_V3_CACHE
          - DEEPSEEK_V3_HF_MODEL
  status: EXPERIMENTAL
  has_builtin_warmup: true
  metadata:
    deepseek-ai/DeepSeek-R1-0528:
      reasoning_parser_name: deepseek_r1
      tool_call_parser_name: deepseek_v3

- weights:
    - meta-llama/Llama-3.3-70B-Instruct
    - meta-llama/Llama-3.1-70B
    - meta-llama/Llama-3.1-70B-Instruct
    - deepseek-ai/DeepSeek-R1-Distill-Llama-70B
  impl: tt_transformers
  system_requirements:
    firmware:
      specifier: ">=18.8.0"
      mode: SUGGESTED
    kmd:
      specifier: ">=2.2.0"
      mode: SUGGESTED
  inference_engine: VLLM
  device_model_specs:
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      override_tt_config:
        trace_region_size: 30000000
      env_vars:
        MAX_PREFILL_CHUNK_SIZE: "32"
        VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  status: FUNCTIONAL
  metadata:
    meta-llama/Llama-3.3-70B-Instruct:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-70B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-70B-Instruct:
      tool_call_parser_name: llama3_json
    deepseek-ai/DeepSeek-R1-Distill-Llama-70B:
      reasoning_parser_name: deepseek_r1
      tool_call_parser_name: deepseek_v3

- weights:
    - meta-llama/Llama-3.3-70B-Instruct
    - meta-llama/Llama-3.1-70B
    - meta-llama/Llama-3.1-70B-Instruct
    - deepseek-ai/DeepSeek-R1-Distill-Llama-70B
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: P150X4
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      override_tt_config:
        trace_region_size: 30000000
      system_requirements:
        firmware:
          specifier: ">=18.5.0"
          mode: STRICT
        kmd:
          specifier: ">=2.3.0"
          mode: STRICT
    - device: P150X8
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      override_tt_config:
        trace_region_size: 268435456
      system_requirements:
        firmware:
          specifier: ">=18.12.0"
          mode: STRICT
        kmd:
          specifier: ">=2.4.1"
          mode: STRICT
  status: FUNCTIONAL
  metadata:
    meta-llama/Llama-3.3-70B-Instruct:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-70B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-70B-Instruct:
      tool_call_parser_name: llama3_json
    deepseek-ai/DeepSeek-R1-Distill-Llama-70B:
      reasoning_parser_name: deepseek_r1
      tool_call_parser_name: deepseek_v3

- weights:
    - meta-llama/Llama-3.3-70B-Instruct
    - meta-llama/Llama-3.1-70B
    - meta-llama/Llama-3.1-70B-Instruct
    - deepseek-ai/DeepSeek-R1-Distill-Llama-70B
  impl: tt_transformers
  system_requirements:
    firmware:
      specifier: ">=19.2.0"
      mode: STRICT
    kmd:
      specifier: ">=2.5.0"
      mode: STRICT
  inference_engine: VLLM
  device_model_specs:
    - device: P300X2
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      override_tt_config:
        trace_region_size: 402653184  # 384 * 1024 * 1024
      known_issues:
        - workflow_type: SPEC_TESTS
          task_name: test_penalties
          reason: "test_penalties length-change assertion (server_tests/test_cases/test_vllm_chat_completions.py:289) is over-strict for short, low-repetition prompts decoded near-greedily; presence_penalty=1.2 legitimately produced identical output. Model meets FUNCTIONAL expectations. Tracked in #3888."
  status: FUNCTIONAL
  has_builtin_warmup: true
  metadata:
    meta-llama/Llama-3.3-70B-Instruct:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-70B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-70B-Instruct:
      tool_call_parser_name: llama3_json
    deepseek-ai/DeepSeek-R1-Distill-Llama-70B:
      reasoning_parser_name: deepseek_r1
      tool_call_parser_name: deepseek_v3

- weights:
    - meta-llama/Llama-3.3-70B-Instruct
    - meta-llama/Llama-3.1-70B
    - meta-llama/Llama-3.1-70B-Instruct
    - deepseek-ai/DeepSeek-R1-Distill-Llama-70B
  impl: tt_transformers
  system_requirements:
    firmware:
      specifier: ">=18.6.0"
      mode: STRICT
    kmd:
      specifier: ">=2.1.0"
      mode: STRICT
  inference_engine: VLLM
  device_model_specs:
    - device: GALAXY_T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      env_vars:
        TT_MM_THROTTLE_PERF: 5
        MAX_PREFILL_CHUNK_SIZE: "32"
        VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/t3k_mesh_graph_descriptor.textproto"
      override_tt_config:
        fabric_config: FABRIC_1D
  status: FUNCTIONAL
  metadata:
    meta-llama/Llama-3.3-70B-Instruct:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-70B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-70B-Instruct:
      tool_call_parser_name: llama3_json
    deepseek-ai/DeepSeek-R1-Distill-Llama-70B:
      reasoning_parser_name: deepseek_r1
      tool_call_parser_name: deepseek_v3

- weights:
    - meta-llama/Llama-3.2-1B
    - meta-llama/Llama-3.2-1B-Instruct
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: N150
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
    - device: N300
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  status: FUNCTIONAL
  has_builtin_warmup: true
  metadata:
    meta-llama/Llama-3.2-1B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.2-1B-Instruct:
      tool_call_parser_name: llama3_json

- weights:
    - meta-llama/Llama-3.2-3B
    - meta-llama/Llama-3.2-3B-Instruct
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: N150
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 10000000
    - device: N300
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
  status: FUNCTIONAL
  metadata:
    meta-llama/Llama-3.2-3B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.2-3B-Instruct:
      tool_call_parser_name: llama3_json

- weights:
    - meta-llama/Llama-3.1-8B
    - meta-llama/Llama-3.1-8B-Instruct
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: N150
      max_concurrency: 32
      max_context: 32768  # 32 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 52000000
    - device: N300
      max_concurrency: 32
      max_context: 32768  # 32 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 70000000
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 117000000
    - device: GPU
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
  status: COMPLETE
  metadata:
    meta-llama/Llama-3.1-8B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-8B-Instruct:
      tool_call_parser_name: llama3_json

- weights:
    - meta-llama/Llama-3.1-8B
    - meta-llama/Llama-3.1-8B-Instruct
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: P100
      max_concurrency: 32
      max_context: 65536  # 64 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 90000000
    - device: P150
      max_concurrency: 32
      max_context: 65536  # 64 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 900000000  # match P100 (single BH chip); 50MB default too small for prefill trace (#4005)
      known_issues:
        - workflow_type: EVALS
          task_name: meta_ifeval
          reason: "meta_ifeval measured scores flakily pass the >=95% threshold https://github.com/tenstorrent/tt-inference-server/issues/4360#issuecomment-4799932610."
        - workflow_type: EVALS
          task_name: meta_gpqa_cot
          reason: "meta_gpqa_cot measured scores flakily pass the >=95% threshold https://github.com/tenstorrent/tt-inference-server/issues/4360#issuecomment-4799932610."
        - workflow_type: EVALS
          task_name: longbench_fewshot_e
          reason: "longbench_fewshot_e known issue with longbench code sub-task where the chat templating interferes with prompt templating https://github.com/tenstorrent/tt-inference-server/issues/4002#issuecomment-4708954121. Masked due to other longbench subtasks passing."
        - workflow_type: EVALS
          task_name: longbench_summarization_e
          reason: "longbench_summarization_e measured score is ~94% which does not satisfy the >=95% threshold  https://github.com/tenstorrent/tt-inference-server/issues/4002#issuecomment-4708954121. Masked due to other longbench subtasks passing."
        - workflow_type: EVALS
          task_name: longbench_code_e
          reason: "longbench_code_e known issue with longbench code sub-task where the chat templating interferes with prompt templating https://github.com/tenstorrent/tt-inference-server/issues/4002#issuecomment-4708954121. Masked due to other longbench subtasks passing."
  status: EXPERIMENTAL
  metadata:
    meta-llama/Llama-3.1-8B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-8B-Instruct:
      tool_call_parser_name: llama3_json

- weights:
    - meta-llama/Llama-3.1-8B
    - meta-llama/Llama-3.1-8B-Instruct
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: P150X4
      max_concurrency: 128  # 32 * 4
      max_context: 131072  # 128 * 1024
      default_impl: true
      vllm_args:
        data_parallel_size: 4
      override_tt_config:
        sample_on_device_mode: decode_only
        trace_region_size: 70000000
  status: COMPLETE
  metadata:
    meta-llama/Llama-3.1-8B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-8B-Instruct:
      tool_call_parser_name: llama3_json

- weights:
    - meta-llama/Llama-3.1-8B
    - meta-llama/Llama-3.1-8B-Instruct
  impl: tt_transformers
  system_requirements:
    firmware:
      specifier: ">=18.12.0"
      mode: STRICT
    kmd:
      specifier: ">=2.4.1"
      mode: STRICT
  inference_engine: VLLM
  device_model_specs:
    - device: P150X8
      max_concurrency: 256  # 32 * 8
      max_context: 131072  # 128 * 1024
      default_impl: true
      vllm_args:
        data_parallel_size: 8
      override_tt_config:
        sample_on_device_mode: decode_only
  status: FUNCTIONAL
  metadata:
    meta-llama/Llama-3.1-8B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-8B-Instruct:
      tool_call_parser_name: llama3_json

- weights:
    - meta-llama/Llama-3.1-8B
    - meta-llama/Llama-3.1-8B-Instruct
  impl: tt_transformers
  system_requirements:
    firmware:
      specifier: ">=19.2.0"
      mode: STRICT
    kmd:
      specifier: ">=2.5.0"
      mode: STRICT
  inference_engine: VLLM
  device_model_specs:
    - device: P300X2
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      override_tt_config:
        sample_on_device_mode: decode_only
        trace_region_size: 155000000
      known_issues:
        - workflow_type: EVALS
          task_name: meta_ifeval
          reason: "meta_ifeval measured scores flakily pass the >=95% threshold https://github.com/tenstorrent/tt-inference-server/issues/4360#issuecomment-4799932610."
        - workflow_type: EVALS
          task_name: meta_gpqa_cot
          reason: "meta_gpqa_cot measured scores flakily pass the >=95% threshold https://github.com/tenstorrent/tt-inference-server/issues/4360#issuecomment-4799932610."
        - workflow_type: EVALS
          task_name: longbench_code_e
          reason: "longbench_code_e known issue with longbench code sub-task where the chat templating interferes with prompt templating https://github.com/tenstorrent/tt-inference-server/issues/4002#issuecomment-4708954121. Masked due to other longbench subtasks passing."
        - workflow_type: EVALS
          task_name: longbench_fewshot_e
          reason: "longbench_fewshot_e known issue with longbench code sub-task where the chat templating interferes with prompt templating https://github.com/tenstorrent/tt-inference-server/issues/4002#issuecomment-4708954121. Masked due to other longbench subtasks passing."
        - workflow_type: EVALS
          task_name: longbench_summarization_e
          reason: "longbench_summarization_e measured score is ~94% which does not satisfy the >=95% threshold  https://github.com/tenstorrent/tt-inference-server/issues/4002#issuecomment-4708954121. Masked due to other longbench subtasks passing."
    - device: P300
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/p300_mesh_graph_descriptor.textproto"
      override_tt_config:
        sample_on_device_mode: decode_only
        trace_region_size: 56000000
      known_issues:
        - workflow_type: EVALS
          task_name: meta_ifeval
          reason: "meta_ifeval measured scores flakily pass the >=95% threshold https://github.com/tenstorrent/tt-inference-server/issues/4360#issuecomment-4799932610."
        - workflow_type: EVALS
          task_name: meta_gpqa_cot
          reason: "meta_gpqa_cot measured scores flakily pass the >=95% threshold https://github.com/tenstorrent/tt-inference-server/issues/4360#issuecomment-4799932610."
        - workflow_type: EVALS
          task_name: longbench_code_e
          reason: "longbench_code_e known issue with longbench code sub-task where the chat templating interferes with prompt templating https://github.com/tenstorrent/tt-inference-server/issues/4002#issuecomment-4708954121. Masked due to other longbench subtasks passing."
        - workflow_type: EVALS
          task_name: longbench_fewshot_e
          reason: "longbench_fewshot_e known issue with longbench code sub-task where the chat templating interferes with prompt templating https://github.com/tenstorrent/tt-inference-server/issues/4002#issuecomment-4708954121. Masked due to other longbench subtasks passing."
        - workflow_type: EVALS
          task_name: longbench_summarization_e
          reason: "longbench_summarization_e measured score is ~94% which does not satisfy the >=95% threshold  https://github.com/tenstorrent/tt-inference-server/issues/4002#issuecomment-4708954121. Masked due to other longbench subtasks passing."
  status: FUNCTIONAL
  metadata:
    meta-llama/Llama-3.1-8B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-8B-Instruct:
      tool_call_parser_name: llama3_json

- weights:
    - meta-llama/Llama-3.1-8B
    - meta-llama/Llama-3.1-8B-Instruct
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: GALAXY
      max_concurrency: 128  # 32 * 4
      max_context: 131072  # 128 * 1024
      default_impl: true
      vllm_args:
        data_parallel_size: 4
      override_tt_config:
        trace_region_size: 50000000
        sample_on_device_mode: all
      env_vars:
        TT_MM_THROTTLE_PERF: 5
    - device: GALAXY_T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        trace_region_size: 50000000
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/t3k_mesh_graph_descriptor.textproto"
        TT_MM_THROTTLE_PERF: 5
      override_tt_config:
        fabric_config: FABRIC_1D
  system_requirements:
    firmware:
      specifier: ">=18.6.0"
      mode: STRICT
    kmd:
      specifier: ">=2.1.0"
      mode: STRICT
  status: FUNCTIONAL
  has_builtin_warmup: true
  metadata:
    meta-llama/Llama-3.1-8B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-8B-Instruct:
      tool_call_parser_name: llama3_json

- weights:
    - Qwen/Qwen2.5-Coder-32B-Instruct
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 56000000
    - device: GALAXY_T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        TT_MM_THROTTLE_PERF: 5
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/t3k_mesh_graph_descriptor.textproto"
      override_tt_config:
        fabric_config: FABRIC_1D
  status: EXPERIMENTAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    Qwen/Qwen2.5-Coder-32B-Instruct:
      tool_call_parser_name: hermes

# =============================================================================
# Gemma 4 family - GPU reference specs (bring-your-own vLLM server; see
# docs/gpu_workflows.md). Used to collect GPU reference eval scores for
# GPQA-Diamond, Terminal-Bench 2.0, and SWE-Bench Verified. These are dev-only
# entries; the dev catalog does not require tt_metal_commit/version pins (the
# GPU bring-your-own-server path doesn't build a tt-metal docker image).
# =============================================================================
- weights:
    - google/gemma-4-26B-A4B-it
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: GPU
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
  status: EXPERIMENTAL
  metadata:
    google/gemma-4-26B-A4B-it:
      reasoning_parser_name: gemma4
      tool_call_parser_name: gemma4

- weights:
    - google/gemma-4-E4B-it
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: GPU
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
  status: EXPERIMENTAL
  metadata:
    google/gemma-4-E4B-it:
      reasoning_parser_name: gemma4
      tool_call_parser_name: gemma4

- weights:
    - google/gemma-4-E2B-it
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: GPU
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
  status: EXPERIMENTAL
  metadata:
    google/gemma-4-E2B-it:
      reasoning_parser_name: gemma4
      tool_call_parser_name: gemma4

# Remote-served model: evaluated against an external OpenAI-compatible
# endpoint via `--server-url` (e.g. Tenstorrent console). The device entry
# is a placeholder so the model resolves through MODEL_SPECS / EVAL_CONFIGS;
# no local TT serving is implied.
- weights:
    - moonshotai/Kimi-K2.6
  impl: tt_transformers
  inference_engine: MEDIA
  device_model_specs:
    - device: SUPER_CLUSTER
      max_concurrency: 64
      max_context: 262144  # 256 * 1024; 
      default_impl: true
  status: EXPERIMENTAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    moonshotai/Kimi-K2.6:
      remote_api_model: moonshotai/Kimi-K2.6
      reasoning_parser_name: kimi_k2
      tool_call_parser_name: kimi_k2
      # Kimi ships a custom tokenizer; AIPerf-driven sweeps (prefix-cache,
      # spec-decode) must trust remote code to load it.
      tokenizer_trust_remote_code: true

# Remote-served model: evaluated against an external OpenAI-compatible
# endpoint via `--server-url` (e.g. Tenstorrent console). The  device entry
# is a placeholder so the model resolves through MODEL_SPECS / EVAL_CONFIGS;
# no local TT serving is implied.
- weights:
    - MiniMaxAI/MiniMax-M2.7
  impl: tt_transformers
  inference_engine: MEDIA
  device_model_specs:
    - device: SUPER_CLUSTER
      max_concurrency: 64
      max_context: 204800  # 200 * 1024; 
      default_impl: true
  status: EXPERIMENTAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    MiniMaxAI/MiniMax-M2.7:
      remote_api_model: MiniMaxAI/MiniMax-M2.7
      reasoning_parser_name: minimax_m2
      tool_call_parser_name: minimax_m2

- weights:
    - openai/gpt-oss-120b
  impl: gpt_oss
  inference_engine: MEDIA
  device_model_specs:
    - device: SUPER_CLUSTER
      max_concurrency: 38
      max_context: 131072  # 128 * 1024
      default_impl: true
  status: EXPERIMENTAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    openai/gpt-oss-120b:
      remote_api_model: openai/gpt-oss-120b
      reasoning_parser_name: openai_gptoss
      tool_call_parser_name: openai

- weights:
    - moonshotai/Kimi-K2.7-Code
  impl: tt_transformers
  inference_engine: MEDIA
  device_model_specs:
    - device: SUPER_CLUSTER
      max_concurrency: 64
      max_context: 262144  # 256 * 1024; 
      default_impl: true
  status: EXPERIMENTAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    moonshotai/Kimi-K2.7-Code:
      remote_api_model: moonshotai/Kimi-K2.7-Code
      reasoning_parser_name: kimi_k2
      tool_call_parser_name: kimi_k2
      # Kimi ships a custom tokenizer; AIPerf-driven sweeps (prefix-cache,
      # spec-decode) must trust remote code to load it.
      tokenizer_trust_remote_code: true

- weights:
    - MiniMaxAI/MiniMax-M3
  impl: tt_transformers
  inference_engine: MEDIA
  device_model_specs:
    - device: SUPER_CLUSTER
      max_concurrency: 64
      max_context: 1048576  # 1024 * 1024; 
      default_impl: true
  status: EXPERIMENTAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    MiniMaxAI/MiniMax-M3:
      remote_api_model: MiniMaxAI/MiniMax-M3
      reasoning_parser_name: minimax_m3
      tool_call_parser_name: minimax_m3
