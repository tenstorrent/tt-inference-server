# Model spec catalog: LLM templates.
# Edit this file to add or modify a LLM model spec.
# Schema: workflows/model_spec.py (ModelSpecTemplate, DeviceModelSpec).
templates:
# =============================================================================
# LLM templates
# =============================================================================
- weights:
    - openai/gpt-oss-20b
  impl: gpt_oss
  inference_engine: VLLM
  device_model_specs:
    - device: T3K
      max_concurrency: 1
      max_context: 16384  # 16 * 1024
      default_impl: true
      env_vars:
        VLLM_ENABLE_RESPONSES_API_STORE: 1
        VLLM_GPT_OSS_HARMONY_SYSTEM_INSTRUCTIONS: 1
    - device: GALAXY_T3K
      max_concurrency: 1
      max_context: 16384  # 16 * 1024
      default_impl: true
      env_vars:
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/t3k_mesh_graph_descriptor.textproto"
    - device: GALAXY
      max_concurrency: 128  # 32 * 4
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        MESH_DEVICE: "(4, 8)"  # Override default TG->(8,4) to use (4,8) mesh grid
      vllm_args:
        data_parallel_size: 4
  status: EXPERIMENTAL
  has_builtin_warmup: true
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    openai/gpt-oss-20b:
      reasoning_parser_name: openai_gptoss
      tool_call_parser_name: openai

- weights:
    - openai/gpt-oss-120b
  impl: gpt_oss
  inference_engine: VLLM
  device_model_specs:
    - device: T3K
      max_concurrency: 1
      max_context: 16384  # 16 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      override_tt_config:
        trace_region_size: 134217728  # 128 MB; vLLM default 50 MB is too small for long prefill
    - device: GALAXY
      max_concurrency: 32  # currently limiting client-side max_concurrency=32 to allow workflows to
      # complete else they will timeout due to hitting the vLLM RPC recv 30min timeout
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      env_vars:
        MESH_DEVICE: "(4, 8)"  # Override default TG->(8,4) to use (4,8) mesh grid
        TT_MM_THROTTLE_PERF: 2
      vllm_args:
        data_parallel_size: 4
        max_num_seqs: 32  # override the default inferred by max_concurrency
      override_tt_config:
        sample_on_device_mode: all
        trace_region_size: 134217728  # 128 MB; vLLM default 50 MB is too small for long prefill
      known_issues:
        - workflow_type: EVALS
          task_name: aime25
          reason: "Model implementation is producing non-deterministic outputs causing this eval task to pass/fail flakily. Masking this part of the acceptance criteria due to other evals passing consistently."
        - workflow_type: EVALS
          task_name: gpqa_diamond_cot_zeroshot
          reason: "Model implementation is producing non-deterministic outputs causing this eval task to pass/fail flakily. Masking this part of the acceptance criteria due to other evals passing consistently."
  status: EXPERIMENTAL
  has_builtin_warmup: true
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    openai/gpt-oss-120b:
      reasoning_parser_name: openai_gptoss
      tool_call_parser_name: openai

- weights:
    - openai/gpt-oss-120b
  impl: gpt_oss
  inference_engine: VLLM
  device_model_specs:
    - device: P300X2
      max_concurrency: 1
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      env_vars:
        MESH_DEVICE: "(1, 4)"
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/p300_x2_mesh_graph_descriptor.textproto"
      override_tt_config:
        trace_region_size: 58000000
        sample_on_device_mode: decode_only
      known_issues:
        - workflow_type: EVALS
          task_name: aime25
          reason: "Model implementation is producing non-deterministic outputs causing this eval task to pass/fail flakily. Masking this part of the acceptance criteria due to other evals passing consistently."
        - workflow_type: EVALS
          task_name: gpqa_diamond_cot_zeroshot
          reason: "Model implementation is producing non-deterministic outputs causing this eval task to pass/fail flakily. Masking this part of the acceptance criteria due to other evals passing consistently."
  status: EXPERIMENTAL
  has_builtin_warmup: true
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    openai/gpt-oss-120b:
      reasoning_parser_name: openai_gptoss
      tool_call_parser_name: openai

- weights:
    - arcee-ai/AFM-4.5B
  impl: tt_transformers
  inference_engine: VLLM
  # need to add default sampling params here because they're
  # not in generation_config.json
  # see: https://github.com/tenstorrent/tt-inference-server/issues/1066
  device_model_specs:
    - device: N300
      max_concurrency: 32
      max_context: 65536  # 64 * 1024
      default_impl: true
      vllm_args:
        override_generation_config: '{"temperature": 0.5, "top_k": 50, "top_p": 0.95}'
    - device: T3K
      max_concurrency: 32
      max_context: 65536  # 64 * 1024
      default_impl: true
      vllm_args:
        override_generation_config: '{"temperature": 0.5, "top_k": 50, "top_p": 0.95}'
  status: EXPERIMENTAL

- weights:
    - google/gemma-3-1b-it
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: N150
      max_concurrency: 32
      max_context: 32768  # 32 * 1024
      default_impl: true
      override_tt_config:
        l1_small_size: 4096
        fabric_config: FABRIC_1D
  status: EXPERIMENTAL
  has_builtin_warmup: true

- weights:
    - google/gemma-4-31B-it
  impl: tt_transformers
  # Dev specs omit tt_metal_commit/vllm_commit (base ModelSpecTemplate rejects
  # them; only prod specs carry commits). The image is supplied at runtime via
  # --override-docker-image; the prod gemma4 specs pin the build commits.
  inference_engine: VLLM
  device_model_specs:
    - device: P300X2  # QB2 (2x P300, 4-chip TP mesh)
      # Decode batch ceiling is 32 (metal batch-32 demo). Prefill is still
      # micro-batched ≤4 users inside the generator.
      max_concurrency: 32
      # Metal policy (generator_trace 31B/_QB2): auto-bounded at ≥64k; at
      # ISL≥128k + bounded → chunk 2048 (coherency). Serve 256k with that path.
      # Scheduler tokens must match the resolved metal chunk (2048 here).
      max_context: 262144  # 256 * 1024
      default_impl: true
      env_vars:
        # QB2's 4 Blackhole chips are driven as a P150x4 (1,4) mesh — matches the
        # passing vLLM nightly [BH-QB-2] Gemma4 entry. The custom p300_x2 descriptor
        # laid the TP collectives over the wrong fabric links and corrupted decode
        # logits; the default p150_x4 descriptor (selected from MESH_DEVICE) is correct.
        MESH_DEVICE: "P150x4"
        GEMMA4_CCL_TOPOLOGY: "linear"
        GEMMA4_PAGE_BLOCK_SIZE: "64"
        GEMMA4_MAX_TOKENS_ALL_USERS: "262144"
        GEMMA4_VLLM_SINGLE_CHUNK: "0"
        # Trace middle-chunk (sp1) path for long-ISL multi-chunk replay.
        GEMMA4_CHUNKED_PREFILL_TRACE: "1"
      override_tt_config:
        fabric_config: FABRIC_1D
        trace_region_size: 512000000
        l1_small_size: 24576  # all_gather semaphores into L1_SMALL; without it QB2 31B wedges at conc=32 (metal demo parity)
        # On-device decode sampling is required on the TP mesh so token ids
        # >= 65536 are reachable (host sampling only sees device 0's vocab shard).
        sample_on_device_mode: decode_only
      vllm_args:
        # Overlap host schedule with device decode (metal supports_async_decode; #51186).
        async_scheduling: true
        # Sampling params (temp=1.0 / top_k=64 / top_p=0.95) load from the
        # model's HF generation_config.json; evals override per-request. No
        # override_generation_config here -- it would just restate the HF default.
        # Activate the gemma4 tool_call_parser so tool_choice:"auto" clients
        # (mini-swe-agent / SWE-Bench) work on a flag-free release run. vLLM
        # requires --tool-call-parser alongside --enable-auto-tool-choice, so it
        # is passed explicitly here; the gemma4 parser is registered by the
        # vllm-tt-plugin entry point (tenstorrent/vllm #431).
        enable-auto-tool-choice: true
        tool-call-parser: gemma4
        # Enable gemma-4 thinking server-side so the r1_gpqa_diamond /
        # terminal-bench-2 / swe-bench-verified evals (methodology matched to
        # tenstorrent/tt-inference-server#4331) get the reasoning channel the
        # chat template otherwise suppresses (gemma-4 scores ~10pts lower w/o it).
        # --reasoning-parser separates the thought channel into reasoning_content.
        default-chat-template-kwargs: '{"enable_thinking": true}'
        reasoning-parser: gemma4
        # Token-chunked prefill (tenstorrent/vllm#448). Parity with the 12B spec
        # (config-shadows lesson): the earlier 2048/2048 here was the shadow that
        # produced the invalidated "19/32 coherent @ 0.8 tok/s/u" b32 number --
        # chunked 4k prompts hit pre-gate batched prefill and 32x more scheduler
        # rounds starved decode under async scheduling. 65536/4096 measured
        # coherent and faster on QB2/LB (2026-08-31 battery; galaxy re-run 09-01).
        enable_chunked_prefill: true
        max_num_batched_tokens: 65536
        long_prefill_token_threshold: 4096
    # LoudBox (8x P150). Metal policy: unbounded through 64k; auto-bounded at
    # ≥128k (generator_trace). Prefill chunk default 4096; at ≥128k + bounded
    # → 2048. Scheduler tokens stay 2048 to match metal at long ISL. B=32 needs
    # ≥512MB trace region.
    - device: P150X8
      max_concurrency: 32
      max_context: 262144  # 256 * 1024
      default_impl: true
      env_vars:
        MESH_DEVICE: "P150x8"
        GEMMA4_PAGE_BLOCK_SIZE: "64"
        GEMMA4_MAX_TOKENS_ALL_USERS: "262144"
        GEMMA4_VLLM_SINGLE_CHUNK: "0"
        # Explicitly clear demo single-chunk so a polluted parent shell cannot
        # force full-ISL warmup (hangs at 128k/256k on LB).
        GEMMA4_CHUNKED_PREFILL_TRACE: "1"
      override_tt_config:
        fabric_config: FABRIC_1D
        trace_region_size: 512000000
        l1_small_size: 24576  # all_gather semaphores into L1_SMALL; without it QB2 31B wedges at conc=32 (metal demo parity)
        # On-device decode sampling required on TP so token ids >= 65536 are
        # reachable (host sampling only sees device 0's vocab shard).
        sample_on_device_mode: decode_only
      vllm_args:
        # Overlap host schedule with device decode (metal supports_async_decode; #51186).
        async_scheduling: true
        enable-auto-tool-choice: true
        tool-call-parser: gemma4
        default-chat-template-kwargs: '{"enable_thinking": true}'
        reasoning-parser: gemma4
        # Token-chunked prefill (tenstorrent/vllm#448). Parity with the 12B spec
        # (config-shadows lesson): the earlier 2048/2048 here was the shadow that
        # produced the invalidated "19/32 coherent @ 0.8 tok/s/u" b32 number --
        # chunked 4k prompts hit pre-gate batched prefill and 32x more scheduler
        # rounds starved decode under async scheduling. 65536/4096 measured
        # coherent and faster on QB2/LB (2026-08-31 battery; galaxy re-run 09-01).
        enable_chunked_prefill: true
        max_num_batched_tokens: 65536
        long_prefill_token_threshold: 4096
    # GPU (H100 NVL) bring-your-own vLLM server (see docs/gpu_workflows.md); used
    # to collect GPU reference eval scores for GPQA-Diamond, Terminal-Bench 2.0,
    # and SWE-Bench Verified. Dev-only; no tt_metal_commit/version pin needed
    # (the bring-your-own-server path builds no tt-metal docker image).
    # WH T3K (1x8 = 8x n300, ~96 GB). Metal policy (generator_trace 31B/T3K):
    # unbounded through 16k, auto-bounded + multi-chunk (2048) from 32k. Measured
    # batch-1 on a real T3K: 4k TTFT 8.5 s / 15.7 tok/s, 32k 55 s / 14.8 tok/s,
    # 128k 188 s / 12.9 tok/s, 256k 564 s / 10.1 tok/s (all PASS). Served context
    # is held at 128k: metal Direct reached 256k, but the vLLM paged pool plus
    # trace regions have not been validated that high on WH.
    - device: T3K
      # Decode batch ceiling 32, same as the Blackhole specs. max_context is the
      # whole-device KV pool (it sets both max_model_len and
      # max_tokens_all_users), so raising concurrency partitions that fixed pool
      # rather than multiplying it — 32 concurrent users share max_context
      # tokens. Wormhole's 12 GB per ASIC therefore constrains max_context
      # below, not max_concurrency here.
      max_concurrency: 32
      # Long-prose ceiling (tt-metal#55102, WH-local): deterministic garbled
      # output at specific encoded lengths >=100k on T3K. 31B verified clean at
      # 100,462 and 104,954; garbled at 100,793 / 109,857 / 127,574. The map is
      # non-monotonic, so no max_context value can express it; treat ~100k as
      # the last verified-clean prose point until the ticket closes.
      max_context: 131072  # validated on T3K; 262144 OOMs (one full-attn K pool = 270532608B x 10 layers x K+V = 5.4GB + 3.87GB weights > 12GB/chip)  # 32 * 1024 — unbounded sliding (multi-request safe)
      default_impl: true
      env_vars:
        MESH_DEVICE: "T3K"
        # Ring is a Blackhole >=8-device default only; WH must stay Linear
        # (models/demos/gemma4/tt/ccl.py default_ccl_topology).
        GEMMA4_CCL_TOPOLOGY: "linear"
        GEMMA4_PAGE_BLOCK_SIZE: "64"
        GEMMA4_MAX_TOKENS_ALL_USERS: "131072"
        GEMMA4_VLLM_SINGLE_CHUNK: "0"
        GEMMA4_CHUNKED_PREFILL_TRACE: "1"
        # KNOWN BAD ON WORMHOLE — concurrent decode returns nondeterministic
        # garbage. Measured on T3K 12B (distinct arithmetic prompts, temp 0,
        # 3 repeats): every request completes, but correctness swings run to run
        # — async OFF gave 4/4, 0/4, 0/4 at concurrency 4 and 1/8, 8/8, 8/8 at
        # concurrency 8; async ON gave 4/4, 0/4, 0/4 and 0/8, 8/8, 3/8. Only
        # concurrency 1 is stable. So max_concurrency: 32 below is the BH-parity
        # target, NOT a verified-good setting on Wormhole yet.
        #
        # async decode is pinned OFF here as a precaution, not as a proven fix:
        # across those repeats it was never worse than ON and was the only
        # config with a clean 3/3 at concurrency 1, but n=3 cannot separate it
        # from the underlying corruption. The BH async mitigations (async-ahead
        # merge, preempt gen accounting, page-table -1 pad) are BH-validated
        # only. Revisit both this flag and max_concurrency once the concurrent
        # decode corruption is root-caused on WH.
        GEMMA4_SUPPORTS_ASYNC_DECODE: "0"
      override_tt_config:
        fabric_config: FABRIC_1D
        # WH cannot afford the BH 512 MB trace region; 90 MB matches the other
        # T3K LLM specs and the raised gemma4 WH demo default. Note this only
        # fits the default {B=1, B=max} decode-trace warmup — capturing every
        # power-of-two bucket overflows it (mesh_trace.cpp TT_FATAL).
        trace_region_size: 200000000
        # On-device decode sampling is required on a TP mesh so token ids
        # >= 65536 are reachable (host sampling only sees device 0's shard).
        sample_on_device_mode: decode_only
      vllm_args:
        # No async_scheduling on Wormhole. Measured on T3K 12B at max_num_seqs 32:
        # async ON gives 32/32 correct on only 4 of 6 reps (30/32 on the other two,
        # degenerate repetition) at 377-389 tok/s aggregate, while async OFF gives
        # 32/32 on four consecutive reps at 363-383 tok/s. Identical throughput,
        # strictly worse correctness — so it is pure downside here. The platform
        # also disables it automatically once the model reports
        # supports_async_decode=False (GEMMA4_SUPPORTS_ASYNC_DECODE=0 above).
        # Re-enable together with that env once the WH async path is fixed.
        enable-auto-tool-choice: true
        tool-call-parser: gemma4
        default-chat-template-kwargs: '{"enable_thinking": true}'
        reasoning-parser: gemma4
        # Token-chunked prefill (tenstorrent/vllm#448). BH/WH parity values
        # (standing policy #1, T3K reconfigured to full BH parity 2026-08-31):
        # the parity re-run banked 9x b32@4k throughput (1.26 -> 11.7 tok/s/u)
        # and cleared the corruption bands the old 2048/2048 scheduler shadow
        # had masked.
        enable_chunked_prefill: true
        max_num_batched_tokens: 65536
        long_prefill_token_threshold: 4096
    - device: GPU
      max_concurrency: 32
      max_context: 204800  # 200 * 1024; native ctx is 256K but a single H100 NVL (94GB, bf16 KV) caps servable ctx at ~205K (KV cache = 210,605 tokens)
      default_impl: true
  status: EXPERIMENTAL
  has_builtin_warmup: true
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    google/gemma-4-31B-it:
      reasoning_parser_name: gemma4
      # tool parser ships in vllm-tt-plugin as of tenstorrent/vllm #431; activated
      # by enable-auto-tool-choice in vllm_args above.
      tool_call_parser_name: gemma4

- weights:
    - google/gemma-4-12B-it
  impl: tt_transformers
  # Dev specs omit tt_metal_commit/vllm_commit (base ModelSpecTemplate rejects
  # them; only prod specs carry commits). The image is supplied at runtime via
  # --override-docker-image; the prod gemma4 specs pin the build commits.
  inference_engine: VLLM
  device_model_specs:
    - device: P300X2  # QB2 (2x P300, 4-chip TP mesh)
      max_concurrency: 32
      # Metal policy (generator_trace 12B/_QB2): unbounded through 128k; auto
      # bounded + multi-chunk (4096) at 256k (unbounded 256k OOM). Match demo.
      max_context: 262144  # 256 * 1024
      default_impl: true
      env_vars:
        # QB2's 4 Blackhole chips are driven as a P150x4 (1,4) mesh — matches the
        # passing vLLM nightly [BH-QB-2] Gemma4 entry. The custom p300_x2 descriptor
        # laid the TP collectives over the wrong fabric links and corrupted decode
        # logits; the default p150_x4 descriptor (selected from MESH_DEVICE) is correct.
        MESH_DEVICE: "P150x4"
        GEMMA4_CCL_TOPOLOGY: "linear"
        GEMMA4_PAGE_BLOCK_SIZE: "64"
        GEMMA4_MAX_TOKENS_ALL_USERS: "262144"
        GEMMA4_VLLM_SINGLE_CHUNK: "0"
        GEMMA4_CHUNKED_PREFILL_TRACE: "1"
      override_tt_config:
        fabric_config: FABRIC_1D
        # B=32 decode traces (match 31B QB2 headroom).
        trace_region_size: 512000000
        l1_small_size: 24576  # all_gather semaphores into L1_SMALL; without it QB2 31B wedges at conc=32 (metal demo parity)
        sample_on_device_mode: decode_only
      vllm_args:
        # Overlap host schedule with device decode (metal supports_async_decode; #51186).
        async_scheduling: true
        # Sampling params (temp=1.0 / top_k=64 / top_p=0.95) load from the
        # model's HF generation_config.json; evals override per-request. No
        # override_generation_config here -- it would just restate the HF default.
        # Activate the gemma4 tool_call_parser so tool_choice:"auto" clients
        # (mini-swe-agent / SWE-Bench) work on a flag-free release run. vLLM
        # requires --tool-call-parser alongside --enable-auto-tool-choice, so it
        # is passed explicitly here; the gemma4 parser is registered by the
        # vllm-tt-plugin entry point (tenstorrent/vllm #431).
        enable-auto-tool-choice: true
        tool-call-parser: gemma4
        # Enable gemma-4 thinking server-side so the r1_gpqa_diamond /
        # terminal-bench-2 / swe-bench-verified evals (methodology matched to
        # tenstorrent/tt-inference-server#4331) get the reasoning channel the
        # chat template otherwise suppresses (gemma-4 scores ~10pts lower w/o it).
        # --reasoning-parser separates the thought channel into reasoning_content.
        default-chat-template-kwargs: '{"enable_thinking": true}'
        reasoning-parser: gemma4
        # Token-chunked prefill (tenstorrent/vllm#448). Match metal 4096.
        enable_chunked_prefill: true
        max_num_batched_tokens: 65536
        long_prefill_token_threshold: 4096
    # LoudBox: metal isl_sweep unbounded through 256k PASS for 12B; multi-chunk
    # prefill (4096) matches demo policy (no auto-bound until >256k).
    - device: P150X8
      max_concurrency: 32
      max_context: 262144  # 256 * 1024
      default_impl: true
      env_vars:
        MESH_DEVICE: "P150x8"
        # 2026-09-02 prefill-config flip — THIS ENTRY ONLY (12B/P150x8):
        # gated sequential+traced prefill deterministically wedges this
        # quadrant under mixed prefill+decode (G7: 512-bucket trace replay,
        # NOC1 stall at the sampling eltwise; benchmark sweep cannot finish).
        # Bounded sliding disables prefill traces (no wedge class; ~87%
        # sliding-KV freed) and batched prefill recovers short-ISL conc32
        # TTFT (-68..-77%); full target sweep 24/24 at gated perf parity.
        # KNOWN ISSUE (all bounded configs, open): ~1-3/32 outputs at
        # conc32 small-ISL bursts after a prior wave show near-miss/stale
        # corruption (wave-boundary ring/trace staleness family; repro:
        # ~/g4cov/wave_smoke.sh). Gated cannot serve these shapes at all,
        # so this is a strict availability improvement for this quadrant.
        # Other gemma-4 BH entries stay gated until that issue is fixed.
        G4_FORCE_BATCH_PREFILL: "1"
        GEMMA4_BOUNDED_SLIDING_KV_CACHE: "1"
        GEMMA4_CCL_TOPOLOGY: "linear"
        GEMMA4_PAGE_BLOCK_SIZE: "64"
        # Shared KV pool; 262144 is ~max that fits P150x8 12B DRAM (524288 OOMs).
        GEMMA4_MAX_TOKENS_ALL_USERS: "262144"
        GEMMA4_VLLM_SINGLE_CHUNK: "0"
        GEMMA4_CHUNKED_PREFILL_TRACE: "0"  # eager chunks: no prefill traces under bounded (see note)
      override_tt_config:
        fabric_config: FABRIC_1D
        trace_region_size: 512000000
        l1_small_size: 24576  # all_gather semaphores into L1_SMALL; without it QB2 31B wedges at conc=32 (metal demo parity)
        # On-device decode sampling required on TP so token ids >= 65536 are
        # reachable (host sampling only sees device 0's vocab shard).
        sample_on_device_mode: decode_only
      vllm_args:
        # Overlap host schedule with device decode (metal supports_async_decode; #51186).
        async_scheduling: true
        enable-auto-tool-choice: true
        tool-call-parser: gemma4
        default-chat-template-kwargs: '{"enable_thinking": true}'
        reasoning-parser: gemma4
        enable_chunked_prefill: true
        # 65536 = 32 users x 2048 ISL in one prefill step. Requires the trace cap
        # aligned to the shared planner (128Ki) so wide prefill stays TRACED;
        # at 4096 the batch-2 steps starve decode (TPOT 281ms -> 35ms measured).
        max_num_batched_tokens: 65536
        long_prefill_token_threshold: 4096
    # GPU (H100 NVL) bring-your-own vLLM server (see docs/gpu_workflows.md); used
    # to collect GPU reference eval scores for GPQA-Diamond, Terminal-Bench 2.0,
    # and SWE-Bench Verified. Dev-only; no tt_metal_commit/version pin needed
    # (the bring-your-own-server path builds no tt-metal docker image).
    # WH T3K (1x8 = 8x n300, ~96 GB). Metal policy (generator_trace 12B/T3K):
    # unbounded through 8k, auto-bounded + multi-chunk (2048) above that. Measured
    # batch-1 on a real T3K: 4k TTFT 9.9 s / 24.0 tok/s, 32k 49 s / 22.5 tok/s,
    # 128k 163 s / 19.1 tok/s, 256k 468 s / 14.3 tok/s — the full HF ISL PASSES.
    - device: T3K
      # Decode batch ceiling 32, same as the Blackhole specs. max_context is the
      # whole-device KV pool (it sets both max_model_len and
      # max_tokens_all_users), so raising concurrency partitions that fixed pool
      # rather than multiplying it — 32 concurrent users share max_context
      # tokens. Wormhole's 12 GB per ASIC therefore constrains max_context
      # below, not max_concurrency here.
      max_concurrency: 32
      # Long-prose ceiling (tt-metal#55102, WH-local): 12B verified clean to
      # ~106k encoded; rep-divergent at ~120k, degenerate at 127,574. See 31B
      # note - non-monotonic map, ~106k is the last verified-clean prose point.
      max_context: 262144  # validated on T3K (bounded sliding): b1@256k coherent, 147s TTFT / 16.4 t/s/u; primes-verified only above ~110k (tt-metal#55102)  # 128 * 1024 — unbounded sliding (see comment)
      default_impl: true
      env_vars:
        MESH_DEVICE: "T3K"
        # 2026-09-03 flip (WH-endorsed after the T3K verification campaign):
        # gated (traced sequential prefill) wedges 12B/T3K deterministically
        # at the first b32@4k conc32 burst (G7 cross-platform; trace-bucket
        # trim does not clear it). Bounded disables prefill traces (zero
        # wedges, full battery x3 boots) and batched prefill engages (b32
        # min-TTFT 1.4s). Known issue documented on the P150X8 entry applies
        # here too: ~1-3/32 corrupt at conc32 small-ISL bursts after a prior
        # wave (wave-boundary staleness; async-independent per WH A/B).
        G4_FORCE_BATCH_PREFILL: "1"
        GEMMA4_BOUNDED_SLIDING_KV_CACHE: "1"
        # Ring is a Blackhole >=8-device default only; WH must stay Linear
        # (models/demos/gemma4/tt/ccl.py default_ccl_topology).
        GEMMA4_GEN_PREFILL_CHUNK: "4096"  # measured on T3K 12B @64k metal A/B: warm TTFT 19.8s (4096) vs 23.6s (2048), cold 35.9 vs 56.4 - matches BH direction; an earlier server-128k reading suggesting 2048 was a cold-capture artifact. NOTE: MAX_PREFILL_CHUNK_SIZE is a decoy knob for gemma4 (read by tt_transformers only); this env is the real one.
        GEMMA4_CCL_TOPOLOGY: "linear"
        GEMMA4_PAGE_BLOCK_SIZE: "64"
        GEMMA4_MAX_TOKENS_ALL_USERS: "262144"
        GEMMA4_VLLM_SINGLE_CHUNK: "0"
        GEMMA4_CHUNKED_PREFILL_TRACE: "0"  # inert under bounded (prefill traces disabled); pinned 0 to mirror the P150X8 entry
        # KNOWN BAD ON WORMHOLE — concurrent decode returns nondeterministic
        # garbage. Measured on T3K 12B (distinct arithmetic prompts, temp 0,
        # 3 repeats): every request completes, but correctness swings run to run
        # — async OFF gave 4/4, 0/4, 0/4 at concurrency 4 and 1/8, 8/8, 8/8 at
        # concurrency 8; async ON gave 4/4, 0/4, 0/4 and 0/8, 8/8, 3/8. Only
        # concurrency 1 is stable. So max_concurrency: 32 below is the BH-parity
        # target, NOT a verified-good setting on Wormhole yet.
        #
        # async decode is pinned OFF here as a precaution, not as a proven fix:
        # across those repeats it was never worse than ON and was the only
        # config with a clean 3/3 at concurrency 1, but n=3 cannot separate it
        # from the underlying corruption. The BH async mitigations (async-ahead
        # merge, preempt gen accounting, page-table -1 pad) are BH-validated
        # only. Revisit both this flag and max_concurrency once the concurrent
        # decode corruption is root-caused on WH.
        GEMMA4_SUPPORTS_ASYNC_DECODE: "0"
      override_tt_config:
        fabric_config: FABRIC_1D
        # WH cannot afford the BH 512 MB trace region; 90 MB matches the other
        # T3K LLM specs and the raised gemma4 WH demo default. Note this only
        # fits the default {B=1, B=max} decode-trace warmup — capturing every
        # power-of-two bucket overflows it (mesh_trace.cpp TT_FATAL).
        trace_region_size: 200000000
        # On-device decode sampling is required on a TP mesh so token ids
        # >= 65536 are reachable (host sampling only sees device 0's shard).
        sample_on_device_mode: decode_only
      vllm_args:
        # No async_scheduling on Wormhole. Measured on T3K 12B at max_num_seqs 32:
        # async ON gives 32/32 correct on only 4 of 6 reps (30/32 on the other two,
        # degenerate repetition) at 377-389 tok/s aggregate, while async OFF gives
        # 32/32 on four consecutive reps at 363-383 tok/s. Identical throughput,
        # strictly worse correctness — so it is pure downside here. The platform
        # also disables it automatically once the model reports
        # supports_async_decode=False (GEMMA4_SUPPORTS_ASYNC_DECODE=0 above).
        # Re-enable together with that env once the WH async path is fixed.
        enable-auto-tool-choice: true
        tool-call-parser: gemma4
        default-chat-template-kwargs: '{"enable_thinking": true}'
        reasoning-parser: gemma4
        # Token-chunked prefill (tenstorrent/vllm#448). BH/WH parity values
        # (standing policy #1, T3K reconfigured to full BH parity 2026-08-31):
        # the parity re-run banked 9x b32@4k throughput (1.26 -> 11.7 tok/s/u)
        # and cleared the corruption bands the old 2048/2048 scheduler shadow
        # had masked.
        enable_chunked_prefill: true
        max_num_batched_tokens: 65536
        long_prefill_token_threshold: 4096
    # WH N300 (1x2 = one n300 card, 24 GB). 12B is the only Gemma4 variant that
    # fits a single WH card — 26B-A4B and 31B OOM here even at 4k. Measured
    # batch-1: 4k TTFT 11.4 s / 12.0 tok/s, 32k 60 s / 12.7 tok/s, 64k 99 s /
    # 12.3 tok/s, 128k 171 s / 11.7 tok/s. 256k OOMs on KV allocation, so 128k
    # is the ceiling here (unlike T3K).
    - device: N300
      # Decode batch ceiling 32, same as the Blackhole specs. max_context is the
      # whole-device KV pool (it sets both max_model_len and
      # max_tokens_all_users), so raising concurrency partitions that fixed pool
      # rather than multiplying it — 32 concurrent users share max_context
      # tokens. Wormhole's 12 GB per ASIC therefore constrains max_context
      # below, not max_concurrency here.
      max_concurrency: 32
      max_context: 8192  # 8 * 1024 — unbounded sliding ceiling on a 24 GB card
      default_impl: true
      env_vars:
        MESH_DEVICE: "N300"
        # Ring is a Blackhole >=8-device default only; WH must stay Linear
        # (models/demos/gemma4/tt/ccl.py default_ccl_topology).
        GEMMA4_CCL_TOPOLOGY: "linear"
        GEMMA4_PAGE_BLOCK_SIZE: "64"
        GEMMA4_MAX_TOKENS_ALL_USERS: "8192"
        GEMMA4_VLLM_SINGLE_CHUNK: "0"
        GEMMA4_CHUNKED_PREFILL_TRACE: "1"
        # KNOWN BAD ON WORMHOLE — concurrent decode returns nondeterministic
        # garbage. Measured on T3K 12B (distinct arithmetic prompts, temp 0,
        # 3 repeats): every request completes, but correctness swings run to run
        # — async OFF gave 4/4, 0/4, 0/4 at concurrency 4 and 1/8, 8/8, 8/8 at
        # concurrency 8; async ON gave 4/4, 0/4, 0/4 and 0/8, 8/8, 3/8. Only
        # concurrency 1 is stable. So max_concurrency: 32 below is the BH-parity
        # target, NOT a verified-good setting on Wormhole yet.
        #
        # async decode is pinned OFF here as a precaution, not as a proven fix:
        # across those repeats it was never worse than ON and was the only
        # config with a clean 3/3 at concurrency 1, but n=3 cannot separate it
        # from the underlying corruption. The BH async mitigations (async-ahead
        # merge, preempt gen accounting, page-table -1 pad) are BH-validated
        # only. Revisit both this flag and max_concurrency once the concurrent
        # decode corruption is root-caused on WH.
        GEMMA4_SUPPORTS_ASYNC_DECODE: "0"
      override_tt_config:
        fabric_config: FABRIC_1D
        # WH cannot afford the BH 512 MB trace region; 90 MB matches the other
        # T3K LLM specs and the raised gemma4 WH demo default. Note this only
        # fits the default {B=1, B=max} decode-trace warmup — capturing every
        # power-of-two bucket overflows it (mesh_trace.cpp TT_FATAL).
        trace_region_size: 200000000
        # NO sample_on_device_mode here (unlike every other Gemma4 spec):
        # Gemma4's vocab is 262144, and on-device sampling needs the per-device
        # shard to fit ttnn topk's 64k width. TP=2 shards to 131072, so
        # Gemma4Model never builds a SamplingGenerator and requesting
        # decode_only killed the engine on the "self.sampling is not None"
        # assert. Host sampling all-gathers the full vocab and is correct here.
      vllm_args:
        # No async_scheduling on Wormhole. Measured on T3K 12B at max_num_seqs 32:
        # async ON gives 32/32 correct on only 4 of 6 reps (30/32 on the other two,
        # degenerate repetition) at 377-389 tok/s aggregate, while async OFF gives
        # 32/32 on four consecutive reps at 363-383 tok/s. Identical throughput,
        # strictly worse correctness — so it is pure downside here. The platform
        # also disables it automatically once the model reports
        # supports_async_decode=False (GEMMA4_SUPPORTS_ASYNC_DECODE=0 above).
        # Re-enable together with that env once the WH async path is fixed.
        enable-auto-tool-choice: true
        tool-call-parser: gemma4
        default-chat-template-kwargs: '{"enable_thinking": true}'
        reasoning-parser: gemma4
        # Token-chunked prefill (tenstorrent/vllm#448). Must equal the chunk the
        # metal policy resolves for this (model, device) — 2048 on every WH board.
        enable_chunked_prefill: true
        max_num_batched_tokens: 2048
        long_prefill_token_threshold: 2048
    - device: GPU
      max_concurrency: 32
      max_context: 204800  # 200 * 1024; native ctx is 256K but a single H100 NVL caps ~205K
      default_impl: true
  status: EXPERIMENTAL
  has_builtin_warmup: true
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    google/gemma-4-12B-it:
      reasoning_parser_name: gemma4
      # tool parser ships in vllm-tt-plugin as of tenstorrent/vllm #431; activated
      # by enable-auto-tool-choice in vllm_args above.
      tool_call_parser_name: gemma4

- weights:
    - google/diffusiongemma-26B-A4B-it
  impl: diffusion_gemma
  inference_engine: VLLM
  device_model_specs:
    - device: P300X2
      max_concurrency: 1
      # Validated on QB2 from short prompts through 250K with one shared 4K
      # prefill program.
      # DeviceModelSpec derives block_size=64, max_num_seqs=1 and
      # max_num_batched_tokens=max_context from these fields.
      max_context: 262144
      default_impl: true
      env_vars:
        MESH_DEVICE: "P150x4"
        VLLM_RPC_TIMEOUT: "1800000"
        VLLM_ENABLE_V1_MULTIPROCESSING: "0"
        DISABLE_METAL_OP_TIMEOUT: "1"
        DG_VLLM_GUMBEL_MODE: "device"
        DG_UPFRONT_CAPTURE: "1"
        DG_MODEL_OWNED_HYBRID_KV: "1"
        DG_UPFRONT_COARSE_PREFILL_BUCKETS: "1"
        DG_UPFRONT_LAZY_PREFILL_RECAPTURE: "1"
        # Reuse one bounded 4K prefill program for every prompt length. This
        # prevents cold bucket recapture after long agent histories fragment DRAM.
        DG_PREFILL_FIXED_CHUNKS: "1"
        DG_PREFILL_CHUNK_SIZE: "4096"
        # A 2048-token ragged-MoE slice needs a 176 MiB embedding output.
        # After a 118-turn Terminal-Bench trial fragmented DRAM, its 23 MiB
        # per-bank allocation killed EngineCore despite 1.30 GiB total free.
        # Halving the bounded inner slice keeps the same per-token computation
        # while reducing that contiguous allocation to 11 MiB per bank.
        DG_PREFILL_RAGGED_CHUNK: "1024"
        DG_DENOISE_REVEAL_PMAX: "262144"
        DG_DENOISE_SLIDING_WINDOW: "1"
        # Warm representative short lengths before capturing the shared program.
        DG_UPFRONT_PREFILL_WARMUP_LENS: "32,64,96"
        DG_TRACE_REGION_SIZE: "3758096384"
      override_tt_config:
        sample_on_device_mode: all
        enable_model_warmup: true
        trace_mode: all
        trace_region_size: 3758096384
      vllm_args:
        generation-config: vllm
        default-chat-template-kwargs: '{"enable_thinking": true}'
        # vLLM 0.24 enables these generative defaults before the TT platform
        # hook runs. DiffusionGemma owns whole-prompt prefill/KV and emits
        # synchronous 256-token blocks, so reject the defaults at CLI parsing.
        no-enable-prefix-caching: true
        no-enable-chunked-prefill: true
        no-async-scheduling: true
        # Deliberately omit --reasoning-parser for scored GPQA runs. vLLM 0.24
        # honors the parser's request to retain <channel|> delimiters, which can
        # move the boxed answer from message.content into message.reasoning.
        # lm-eval scores content only; parser-off preserves the validated answer
        # bytes while keeping the model's thinking chat template enabled.
  status: EXPERIMENTAL
  has_builtin_warmup: true
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    google/diffusiongemma-26B-A4B-it:
      tokenizer_trust_remote_code: true
      output_block_size: 256
      # A request must leave one full output canvas inside max_context.
      max_effective_input_tokens: 261888

- weights:
    - Qwen/Qwen3-8B
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: N150
      max_concurrency: 32
      max_context: 32768  # 32 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 51000000
    - device: N300
      max_concurrency: 32
      max_context: 40960
      default_impl: true
      override_tt_config:
        trace_region_size: 65000000
    - device: T3K
      max_concurrency: 32
      max_context: 40960
      default_impl: true
    - device: GALAXY_T3K
      max_concurrency: 32
      max_context: 40960
      default_impl: true
      env_vars:
        TT_MM_THROTTLE_PERF: 5
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/t3k_mesh_graph_descriptor.textproto"
      override_tt_config:
        fabric_config: FABRIC_1D
    - device: GALAXY
      max_concurrency: 128  # 32 * 4
      max_context: 40960
      default_impl: true
      vllm_args:
        data_parallel_size: 4
      env_vars:
        TT_MM_THROTTLE_PERF: 5
  status: FUNCTIONAL
  metadata:
    Qwen/Qwen3-8B:
      reasoning_parser_name: qwen3
      tool_call_parser_name: hermes

- weights:
    - Qwen/Qwen3-4B
  impl: tt_transformers
  system_requirements:
    firmware:
      specifier: ">=19.2.0"
      mode: STRICT
    kmd:
      specifier: ">=2.5.0"
      mode: STRICT
  inference_engine: VLLM
  device_model_specs:
    # A single P300 card is a two-chip P150x2 mesh.
    - device: P300
      max_concurrency: 16
      max_context: 40960
      default_impl: true
      env_vars:
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/p300_mesh_graph_descriptor.textproto"
      override_tt_config:
        trace_region_size: 402653184  # 384 MiB for retained multi-shape trace captures
  status: EXPERIMENTAL
  metadata:
    Qwen/Qwen3-4B:
      reasoning_parser_name: qwen3
      tool_call_parser_name: hermes

- weights:
    - Qwen/Qwen3-8B
  impl: tt_transformers
  system_requirements:
    firmware:
      specifier: ">=19.2.0"
      mode: STRICT
    kmd:
      specifier: ">=2.5.0"
      mode: STRICT
  inference_engine: VLLM
  device_model_specs:
    - device: P300
      max_concurrency: 32
      max_context: 40960
      default_impl: true
      env_vars:
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/p300_mesh_graph_descriptor.textproto"
  status: FUNCTIONAL
  metadata:
    Qwen/Qwen3-8B:
      reasoning_parser_name: qwen3
      tool_call_parser_name: hermes

- weights:
    - Qwen/Qwen3.6-27B
  impl: qwen36_blackhole
  inference_engine: VLLM
  model_type: LLM
  supported_modalities:
    - text
  device_model_specs:
    - device: P300X2
      max_concurrency: 32
      max_context: 262144
      max_tokens_all_users_override: 525312
      default_impl: true
      env_vars:
        TT_QWEN35_TEXT_VER: qwen36_blackhole
        HF_MODEL: Qwen/Qwen3.6-27B
        MESH_DEVICE: "(1, 4)"
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/p300_x2_mesh_graph_descriptor.textproto"
        QWEN36_MAX_TOKENS_ALL_USERS: "525312"
      vllm_args:
        enable_auto_tool_choice: true
        reasoning_parser: qwen3
        tool_call_parser: qwen3_coder
      override_tt_config:
        fabric_config: FABRIC_1D
        trace_region_size: 1073741824
        l1_small_size: 24576
        sample_on_device_mode: decode_only
    - device: P150X8
      max_concurrency: 32
      max_context: 262144
      max_tokens_all_users_override: 525312
      default_impl: true
      env_vars:
        HF_MODEL: Qwen/Qwen3.6-27B
        MESH_DEVICE: "P150x8"
        QWEN36_MAX_TOKENS_ALL_USERS: "525312"
      vllm_args:
        enable_auto_tool_choice: true
        reasoning_parser: qwen3
        tool_call_parser: qwen3_coder
      override_tt_config:
        fabric_config: FABRIC_1D
        trace_region_size: 1073741824
        l1_small_size: 24576
        sample_on_device_mode: decode_only
    # Same as Gemma4 note, QB2's 4 Blackhole chips (4x p150a) also
    # run as a P150x4 (1,4) mesh.
    # https://github.com/tenstorrent/tt-inference-server/blob/edb99fae3113f7ce10ba51b6ea310ede173b4b93/workflows/model_specs/dev/llm.yaml#L174-L177
    - device: P150X4
      max_concurrency: 32
      max_context: 262144
      max_tokens_all_users_override: 525312
      default_impl: true
      env_vars:
        TT_QWEN35_TEXT_VER: qwen36_blackhole
        HF_MODEL: Qwen/Qwen3.6-27B
        MESH_DEVICE: "P150x4"
        QWEN36_MAX_TOKENS_ALL_USERS: "525312"
      vllm_args:
        enable_auto_tool_choice: true
        reasoning_parser: qwen3
        tool_call_parser: qwen3_coder
      override_tt_config:
        fabric_config: FABRIC_1D
        trace_region_size: 1073741824
        l1_small_size: 24576
        sample_on_device_mode: decode_only
  status: EXPERIMENTAL
  has_builtin_warmup: true
  metadata:
    Qwen/Qwen3.6-27B:
      reasoning_parser_name: qwen3
      tool_call_parser_name: qwen3_coder

- weights:
    - Qwen/Qwen3-32B
  impl: qwen3_32b_galaxy
  inference_engine: VLLM
  device_model_specs:
    - device: GALAXY
      max_concurrency: 32  # 8 * 4
      # NOTE: model natively supports 40K but use this to override max_num_batched_tokens
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
      vllm_args:
        data_parallel_size: 4
        # The json_object grammar is compiled with any_whitespace=True by
        # default, so an unbounded whitespace run is grammar-legal: the model
        # can lock into emitting whitespace (p -> 1.0 within ~10 tokens), never
        # emit the closing '}', and be truncated at max_tokens -- leaving
        # content that does not parse as JSON. Disabling it makes whitespace an
        # illegal continuation, so the closing brace is forced. Costs nothing at
        # inference time; it only changes which tokens the grammar mask permits.
        # backend must be explicit: the validator rejects this under "auto".
        structured-outputs-config: '{"backend": "xgrammar", "disable_any_whitespace": true}'
        enable_auto_tool_choice: true
        reasoning_parser: qwen3
        tool_call_parser: hermes
        # The Qwen3 template defaults enable_thinking=true. With a reasoning
        # parser active that makes plain requests burn max_tokens inside
        # <think> and return content=None, and lets a stop string fire inside
        # the reasoning. Request-level chat_template_kwargs still win, so
        # callers that want thinking just ask for it.
        default_chat_template_kwargs: '{"enable_thinking": false}'
      override_tt_config:
        dispatch_core_axis: col
        sample_on_device_mode: all
        fabric_config: FABRIC_1D_RING
        worker_l1_size: 1344544
        trace_region_size: 184915840
    - device: BLACKHOLE_GALAXY
      max_concurrency: 32  # 8 * 4
      # NOTE: model natively supports 40K but use this to override max_num_batched_tokens
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
      vllm_args:
        data_parallel_size: 4
      override_tt_config:
        dispatch_core_axis: col
        sample_on_device_mode: all
        fabric_config: FABRIC_1D_RING
        worker_l1_size: 1344544
        trace_region_size: 184915840
  system_requirements:
    firmware:
      specifier: ">=18.6.0"
      mode: STRICT
    kmd:
      specifier: ">=2.1.0"
      mode: STRICT
  status: COMPLETE
  has_builtin_warmup: true
  metadata:
    Qwen/Qwen3-32B:
      reasoning_parser_name: qwen3
      tool_call_parser_name: hermes

- weights:
    - Qwen/Qwen3-32B
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 54000000
    - device: GALAXY_T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        TT_MM_THROTTLE_PERF: 5
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/t3k_mesh_graph_descriptor.textproto"
      override_tt_config:
        fabric_config: FABRIC_1D
    - device: GALAXY
      max_concurrency: 128  # 32 * 4
      max_context: 131072  # 128 * 1024
      default_impl: false
      override_tt_config:
        trace_region_size: 66147328
        sample_on_device_mode: decode_only
      vllm_args:
        data_parallel_size: 4
      env_vars:
        TT_MM_THROTTLE_PERF: 5
    - device: BLACKHOLE_GALAXY
      max_concurrency: 128  # 32 * 4
      max_context: 131072  # 128 * 1024
      default_impl: false
      override_tt_config:
        trace_region_size: 66147328
        sample_on_device_mode: decode_only
      vllm_args:
        data_parallel_size: 4
      env_vars:
        TT_MM_THROTTLE_PERF: 5
  status: FUNCTIONAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    Qwen/Qwen3-32B:
      reasoning_parser_name: qwen3
      tool_call_parser_name: hermes

- weights:
    - Qwen/Qwen3-32B
  impl: tt_transformers
  system_requirements:
    firmware:
      specifier: ">=18.12.0"
      mode: STRICT
    kmd:
      specifier: ">=2.4.1"
      mode: STRICT
  inference_engine: VLLM
  device_model_specs:
    - device: P150X8
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 61341696
  status: FUNCTIONAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    Qwen/Qwen3-32B:
      reasoning_parser_name: qwen3
      tool_call_parser_name: hermes

- weights:
    - Qwen/Qwen3-32B
  impl: tt_transformers
  system_requirements:
    firmware:
      specifier: ">=19.2.0"
      mode: STRICT
    kmd:
      specifier: ">=2.5.0"
      mode: STRICT
  inference_engine: VLLM
  device_model_specs:
    - device: P300X2
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 402653184  # 384 * 1024 * 1024
  status: FUNCTIONAL
  has_builtin_warmup: true
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    Qwen/Qwen3-32B:
      reasoning_parser_name: qwen3
      tool_call_parser_name: hermes

- weights:
    - mistralai/Mistral-7B-Instruct-v0.3
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: N150
      max_concurrency: 32
      max_context: 32768  # 32 * 1024
      default_impl: true
    - device: N300
      max_concurrency: 32
      max_context: 32768  # 32 * 1024
      default_impl: true
    - device: T3K
      max_concurrency: 32
      max_context: 32768  # 32 * 1024
      default_impl: true
  status: COMPLETE
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1

- weights:
    - mistralai/Mistral-Small-3.1-24B-Instruct-2503
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 90000000
      vllm_args:
        limit-mm-per-prompt: '{"image": 1}'
        mm-processor-cache-gb: 0
  model_type: VLM
  status: EXPERIMENTAL
  has_builtin_warmup: true
  supported_modalities:
    - text
    - image
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1

- weights:
    - Qwen/QwQ-32B
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
    - device: GALAXY
      max_concurrency: 128  # 32 * 4
      max_context: 131072  # 128 * 1024
      default_impl: true
      vllm_args:
        data_parallel_size: 4
      override_tt_config:
        trace_region_size: 27381760
      env_vars:
        TT_MM_THROTTLE_PERF: 5
    - device: GALAXY_T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        TT_MM_THROTTLE_PERF: 5
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/t3k_mesh_graph_descriptor.textproto"
      override_tt_config:
        fabric_config: FABRIC_1D
  status: FUNCTIONAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    Qwen/QwQ-32B:
      reasoning_parser_name: deepseek_r1
      tool_call_parser_name: hermes

- weights:
    - Qwen/Qwen2.5-72B
    - Qwen/Qwen2.5-72B-Instruct
  model_display_name: Qwen2.5-72B
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      override_tt_config:
        trace_region_size: 66000000
    - device: GALAXY
      max_concurrency: 128  # 32 * 4
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      vllm_args:
        data_parallel_size: 4
      override_tt_config:
        trace_region_size: 66000000
      env_vars:
        TT_MM_THROTTLE_PERF: 5
    - device: GALAXY_T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      override_tt_config:
        trace_region_size: 40000000
        fabric_config: FABRIC_1D
      env_vars:
        TT_MM_THROTTLE_PERF: 5
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/t3k_mesh_graph_descriptor.textproto"
  status: FUNCTIONAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
    MAX_PREFILL_CHUNK_SIZE: "16"
  metadata:
    Qwen/Qwen2.5-72B:
      tool_call_parser_name: hermes
    Qwen/Qwen2.5-72B-Instruct:
      tool_call_parser_name: hermes

- weights:
    - Qwen/Qwen2.5-7B
    - Qwen/Qwen2.5-7B-Instruct
  model_display_name: Qwen2.5-7B
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: N300
      max_concurrency: 16
      max_context: 32768  # 32 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 60000000
    - device: N150X4
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
  status: EXPERIMENTAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    Qwen/Qwen2.5-7B:
      tool_call_parser_name: hermes
    Qwen/Qwen2.5-7B-Instruct:
      tool_call_parser_name: hermes

- weights:
    - meta-llama/Llama-3.3-70B-Instruct
    - meta-llama/Llama-3.1-70B
    - meta-llama/Llama-3.1-70B-Instruct
    - deepseek-ai/DeepSeek-R1-Distill-Llama-70B
  model_display_name: Llama-3.3-70B-Instruct
  impl: llama3_70b_galaxy
  inference_engine: VLLM
  device_model_specs:
    - device: GALAXY
      max_concurrency: 32  # 8 * 4
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      vllm_args:
        data_parallel_size: 4
      override_tt_config:
        dispatch_core_axis: col
        sample_on_device_mode: all
        fabric_config: FABRIC_1D_RING
        worker_l1_size: 1344544
        trace_region_size: 215000000
    - device: BLACKHOLE_GALAXY
      max_concurrency: 32  # 8 * 4
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      vllm_args:
        data_parallel_size: 4
      override_tt_config:
        dispatch_core_axis: col
        sample_on_device_mode: all
        fabric_config: FABRIC_1D_RING
        worker_l1_size: 1344544
        trace_region_size: 215000000
  system_requirements:
    firmware:
      specifier: ">=18.6.0"
      mode: STRICT
    kmd:
      specifier: ">=2.1.0"
      mode: STRICT
  status: COMPLETE
  has_builtin_warmup: true
  metadata:
    meta-llama/Llama-3.3-70B-Instruct:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-70B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-70B-Instruct:
      tool_call_parser_name: llama3_json
    deepseek-ai/DeepSeek-R1-Distill-Llama-70B:
      reasoning_parser_name: deepseek_r1
      tool_call_parser_name: deepseek_v3

- weights:
    - deepseek-ai/DeepSeek-R1-0528
  impl: deepseek_r1_galaxy
  inference_engine: VLLM
  device_model_specs:
    - device: GALAXY
      max_concurrency: 256  # 32 * 8
      max_context: 2048
      default_impl: true
      tensor_cache_timeout: 6400.0
      vllm_args:
        max_model_len: "2048"
    - device: DUAL_GALAXY
      max_concurrency: 256  # 32 * 8; 32 per DP rank * 8 ranks
      max_context: 32768
      default_impl: true
      tensor_cache_timeout: 6400.0
      vllm_args:
        data_parallel_size: 8
        block_size: "32"
      override_tt_config:
        fabric_config: FABRIC_1D
        fabric_reliability_mode: RELAXED_INIT
        trace_mode: none
        env_passthrough:
          - DEEPSEEK_V3_CACHE
          - DEEPSEEK_V3_HF_MODEL
    - device: QUAD_GALAXY
      max_concurrency: 512  # 32 * 16; 32 per DP rank * 16 ranks
      max_context: 32768
      default_impl: true
      vllm_args:
        data_parallel_size: 16
        block_size: "32"
        max_model_len: "2048"
      override_tt_config:
        fabric_config: FABRIC_1D
        fabric_reliability_mode: RELAXED_INIT
        trace_mode: none
        env_passthrough:
          - DEEPSEEK_V3_CACHE
          - DEEPSEEK_V3_HF_MODEL
  status: EXPERIMENTAL
  has_builtin_warmup: true
  metadata:
    deepseek-ai/DeepSeek-R1-0528:
      reasoning_parser_name: deepseek_r1
      tool_call_parser_name: deepseek_v3

- weights:
    - meta-llama/Llama-3.3-70B-Instruct
    - meta-llama/Llama-3.1-70B
    - meta-llama/Llama-3.1-70B-Instruct
    - deepseek-ai/DeepSeek-R1-Distill-Llama-70B
  model_display_name: Llama-3.3-70B-Instruct
  impl: tt_transformers
  system_requirements:
    firmware:
      specifier: ">=18.8.0"
      mode: SUGGESTED
    kmd:
      specifier: ">=2.2.0"
      mode: SUGGESTED
  inference_engine: VLLM
  device_model_specs:
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      override_tt_config:
        trace_region_size: 30000000
      env_vars:
        MAX_PREFILL_CHUNK_SIZE: "32"
        VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  status: FUNCTIONAL
  metadata:
    meta-llama/Llama-3.3-70B-Instruct:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-70B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-70B-Instruct:
      tool_call_parser_name: llama3_json
    deepseek-ai/DeepSeek-R1-Distill-Llama-70B:
      reasoning_parser_name: deepseek_r1
      tool_call_parser_name: deepseek_v3

- weights:
    - meta-llama/Llama-3.3-70B-Instruct
    - meta-llama/Llama-3.1-70B
    - meta-llama/Llama-3.1-70B-Instruct
    - deepseek-ai/DeepSeek-R1-Distill-Llama-70B
  model_display_name: Llama-3.3-70B-Instruct
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: P150X4
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      override_tt_config:
        trace_region_size: 30000000
      system_requirements:
        firmware:
          specifier: ">=18.5.0"
          mode: STRICT
        kmd:
          specifier: ">=2.3.0"
          mode: STRICT
    - device: P150X8
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      override_tt_config:
        trace_region_size: 268435456
      system_requirements:
        firmware:
          specifier: ">=18.12.0"
          mode: STRICT
        kmd:
          specifier: ">=2.4.1"
          mode: STRICT
  status: FUNCTIONAL
  metadata:
    meta-llama/Llama-3.3-70B-Instruct:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-70B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-70B-Instruct:
      tool_call_parser_name: llama3_json
    deepseek-ai/DeepSeek-R1-Distill-Llama-70B:
      reasoning_parser_name: deepseek_r1
      tool_call_parser_name: deepseek_v3

- weights:
    - meta-llama/Llama-3.3-70B-Instruct
    - meta-llama/Llama-3.1-70B
    - meta-llama/Llama-3.1-70B-Instruct
    - deepseek-ai/DeepSeek-R1-Distill-Llama-70B
  model_display_name: Llama-3.3-70B-Instruct
  impl: tt_transformers
  system_requirements:
    firmware:
      specifier: ">=19.2.0"
      mode: STRICT
    kmd:
      specifier: ">=2.5.0"
      mode: STRICT
  inference_engine: VLLM
  device_model_specs:
    - device: P300X2
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      override_tt_config:
        trace_region_size: 402653184  # 384 * 1024 * 1024
      known_issues:
        - workflow_type: SPEC_TESTS
          task_name: test_penalties
          reason: "test_penalties length-change assertion (server_tests/test_cases/test_vllm_chat_completions.py:289) is over-strict for short, low-repetition prompts decoded near-greedily; presence_penalty=1.2 legitimately produced identical output. Model meets FUNCTIONAL expectations. Tracked in #3888."
  status: FUNCTIONAL
  has_builtin_warmup: true
  metadata:
    meta-llama/Llama-3.3-70B-Instruct:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-70B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-70B-Instruct:
      tool_call_parser_name: llama3_json
    deepseek-ai/DeepSeek-R1-Distill-Llama-70B:
      reasoning_parser_name: deepseek_r1
      tool_call_parser_name: deepseek_v3

- weights:
    - meta-llama/Llama-3.3-70B-Instruct
    - meta-llama/Llama-3.1-70B
    - meta-llama/Llama-3.1-70B-Instruct
    - deepseek-ai/DeepSeek-R1-Distill-Llama-70B
  model_display_name: Llama-3.3-70B-Instruct
  impl: tt_transformers
  system_requirements:
    firmware:
      specifier: ">=18.6.0"
      mode: STRICT
    kmd:
      specifier: ">=2.1.0"
      mode: STRICT
  inference_engine: VLLM
  device_model_specs:
    - device: GALAXY_T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      tensor_cache_timeout: 5400.0
      env_vars:
        TT_MM_THROTTLE_PERF: 5
        MAX_PREFILL_CHUNK_SIZE: "32"
        VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/t3k_mesh_graph_descriptor.textproto"
      override_tt_config:
        fabric_config: FABRIC_1D
  status: FUNCTIONAL
  metadata:
    meta-llama/Llama-3.3-70B-Instruct:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-70B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-70B-Instruct:
      tool_call_parser_name: llama3_json
    deepseek-ai/DeepSeek-R1-Distill-Llama-70B:
      reasoning_parser_name: deepseek_r1
      tool_call_parser_name: deepseek_v3

- weights:
    - meta-llama/Llama-3.2-1B
    - meta-llama/Llama-3.2-1B-Instruct
  model_display_name: Llama-3.2-1B
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: N150
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
    - device: N300
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  status: FUNCTIONAL
  has_builtin_warmup: true
  metadata:
    meta-llama/Llama-3.2-1B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.2-1B-Instruct:
      tool_call_parser_name: llama3_json

- weights:
    - meta-llama/Llama-3.2-3B
    - meta-llama/Llama-3.2-3B-Instruct
  model_display_name: Llama-3.2-3B
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: N150
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 10000000
    - device: N300
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
  status: FUNCTIONAL
  metadata:
    meta-llama/Llama-3.2-3B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.2-3B-Instruct:
      tool_call_parser_name: llama3_json

- weights:
    - meta-llama/Llama-3.1-8B
    - meta-llama/Llama-3.1-8B-Instruct
  model_display_name: Llama-3.1-8B
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: N150
      max_concurrency: 32
      max_context: 32768  # 32 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 52000000
    - device: N300
      max_concurrency: 32
      max_context: 32768  # 32 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 70000000
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 117000000
    - device: GPU
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
  status: COMPLETE
  metadata:
    meta-llama/Llama-3.1-8B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-8B-Instruct:
      tool_call_parser_name: llama3_json

- weights:
    - meta-llama/Llama-3.1-8B
    - meta-llama/Llama-3.1-8B-Instruct
  model_display_name: Llama-3.1-8B
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: P100
      max_concurrency: 32
      max_context: 65536  # 64 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 90000000
    - device: P150
      max_concurrency: 32
      max_context: 65536  # 64 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 900000000  # match P100 (single BH chip); 50MB default too small for prefill trace (#4005)
      known_issues:
        - workflow_type: EVALS
          task_name: meta_ifeval
          reason: "meta_ifeval measured scores flakily pass the >=95% threshold https://github.com/tenstorrent/tt-inference-server/issues/4360#issuecomment-4799932610."
        - workflow_type: EVALS
          task_name: meta_gpqa_cot
          reason: "meta_gpqa_cot measured scores flakily pass the >=95% threshold https://github.com/tenstorrent/tt-inference-server/issues/4360#issuecomment-4799932610."
        - workflow_type: EVALS
          task_name: longbench_fewshot_e
          reason: "longbench_fewshot_e known issue with longbench code sub-task where the chat templating interferes with prompt templating https://github.com/tenstorrent/tt-inference-server/issues/4002#issuecomment-4708954121. Masked due to other longbench subtasks passing."
        - workflow_type: EVALS
          task_name: longbench_summarization_e
          reason: "longbench_summarization_e measured score is ~94% which does not satisfy the >=95% threshold  https://github.com/tenstorrent/tt-inference-server/issues/4002#issuecomment-4708954121. Masked due to other longbench subtasks passing."
        - workflow_type: EVALS
          task_name: longbench_code_e
          reason: "longbench_code_e known issue with longbench code sub-task where the chat templating interferes with prompt templating https://github.com/tenstorrent/tt-inference-server/issues/4002#issuecomment-4708954121. Masked due to other longbench subtasks passing."
  status: EXPERIMENTAL
  metadata:
    meta-llama/Llama-3.1-8B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-8B-Instruct:
      tool_call_parser_name: llama3_json

- weights:
    - meta-llama/Llama-3.1-8B
    - meta-llama/Llama-3.1-8B-Instruct
  model_display_name: Llama-3.1-8B
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: P150X4
      max_concurrency: 128  # 32 * 4
      max_context: 131072  # 128 * 1024
      default_impl: true
      vllm_args:
        data_parallel_size: 4
      override_tt_config:
        sample_on_device_mode: decode_only
        trace_region_size: 70000000
  status: COMPLETE
  metadata:
    meta-llama/Llama-3.1-8B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-8B-Instruct:
      tool_call_parser_name: llama3_json

- weights:
    - meta-llama/Llama-3.1-8B
    - meta-llama/Llama-3.1-8B-Instruct
  model_display_name: Llama-3.1-8B
  impl: tt_transformers
  system_requirements:
    firmware:
      specifier: ">=18.12.0"
      mode: STRICT
    kmd:
      specifier: ">=2.4.1"
      mode: STRICT
  inference_engine: VLLM
  device_model_specs:
    - device: P150X8
      max_concurrency: 256  # 32 * 8
      max_context: 131072  # 128 * 1024
      default_impl: true
      vllm_args:
        data_parallel_size: 8
      override_tt_config:
        sample_on_device_mode: decode_only
  status: FUNCTIONAL
  metadata:
    meta-llama/Llama-3.1-8B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-8B-Instruct:
      tool_call_parser_name: llama3_json

- weights:
    - meta-llama/Llama-3.1-8B
    - meta-llama/Llama-3.1-8B-Instruct
  model_display_name: Llama-3.1-8B
  impl: tt_transformers
  system_requirements:
    firmware:
      specifier: ">=19.2.0"
      mode: STRICT
    kmd:
      specifier: ">=2.5.0"
      mode: STRICT
  inference_engine: VLLM
  device_model_specs:
    - device: P300X2
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      override_tt_config:
        sample_on_device_mode: decode_only
        trace_region_size: 155000000
      known_issues:
        - workflow_type: EVALS
          task_name: meta_ifeval
          reason: "meta_ifeval measured scores flakily pass the >=95% threshold https://github.com/tenstorrent/tt-inference-server/issues/4360#issuecomment-4799932610."
        - workflow_type: EVALS
          task_name: meta_gpqa_cot
          reason: "meta_gpqa_cot measured scores flakily pass the >=95% threshold https://github.com/tenstorrent/tt-inference-server/issues/4360#issuecomment-4799932610."
        - workflow_type: EVALS
          task_name: longbench_code_e
          reason: "longbench_code_e known issue with longbench code sub-task where the chat templating interferes with prompt templating https://github.com/tenstorrent/tt-inference-server/issues/4002#issuecomment-4708954121. Masked due to other longbench subtasks passing."
        - workflow_type: EVALS
          task_name: longbench_fewshot_e
          reason: "longbench_fewshot_e known issue with longbench code sub-task where the chat templating interferes with prompt templating https://github.com/tenstorrent/tt-inference-server/issues/4002#issuecomment-4708954121. Masked due to other longbench subtasks passing."
        - workflow_type: EVALS
          task_name: longbench_summarization_e
          reason: "longbench_summarization_e measured score is ~94% which does not satisfy the >=95% threshold  https://github.com/tenstorrent/tt-inference-server/issues/4002#issuecomment-4708954121. Masked due to other longbench subtasks passing."
    - device: P300
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/p300_mesh_graph_descriptor.textproto"
      override_tt_config:
        sample_on_device_mode: decode_only
        trace_region_size: 56000000
      known_issues:
        - workflow_type: EVALS
          task_name: meta_ifeval
          reason: "meta_ifeval measured scores flakily pass the >=95% threshold https://github.com/tenstorrent/tt-inference-server/issues/4360#issuecomment-4799932610."
        - workflow_type: EVALS
          task_name: meta_gpqa_cot
          reason: "meta_gpqa_cot measured scores flakily pass the >=95% threshold https://github.com/tenstorrent/tt-inference-server/issues/4360#issuecomment-4799932610."
        - workflow_type: EVALS
          task_name: longbench_code_e
          reason: "longbench_code_e known issue with longbench code sub-task where the chat templating interferes with prompt templating https://github.com/tenstorrent/tt-inference-server/issues/4002#issuecomment-4708954121. Masked due to other longbench subtasks passing."
        - workflow_type: EVALS
          task_name: longbench_fewshot_e
          reason: "longbench_fewshot_e known issue with longbench code sub-task where the chat templating interferes with prompt templating https://github.com/tenstorrent/tt-inference-server/issues/4002#issuecomment-4708954121. Masked due to other longbench subtasks passing."
        - workflow_type: EVALS
          task_name: longbench_summarization_e
          reason: "longbench_summarization_e measured score is ~94% which does not satisfy the >=95% threshold  https://github.com/tenstorrent/tt-inference-server/issues/4002#issuecomment-4708954121. Masked due to other longbench subtasks passing."
  status: FUNCTIONAL
  metadata:
    meta-llama/Llama-3.1-8B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-8B-Instruct:
      tool_call_parser_name: llama3_json

- weights:
    - meta-llama/Llama-3.1-8B
    - meta-llama/Llama-3.1-8B-Instruct
  model_display_name: Llama-3.1-8B
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: GALAXY
      max_concurrency: 128  # 32 * 4
      max_context: 131072  # 128 * 1024
      default_impl: true
      vllm_args:
        data_parallel_size: 4
      override_tt_config:
        trace_region_size: 50000000
        sample_on_device_mode: all
      env_vars:
        TT_MM_THROTTLE_PERF: 5
    - device: GALAXY_T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        trace_region_size: 50000000
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/t3k_mesh_graph_descriptor.textproto"
        TT_MM_THROTTLE_PERF: 5
      override_tt_config:
        fabric_config: FABRIC_1D
  system_requirements:
    firmware:
      specifier: ">=18.6.0"
      mode: STRICT
    kmd:
      specifier: ">=2.1.0"
      mode: STRICT
  status: FUNCTIONAL
  has_builtin_warmup: true
  metadata:
    meta-llama/Llama-3.1-8B:
      tool_call_parser_name: llama3_json
    meta-llama/Llama-3.1-8B-Instruct:
      tool_call_parser_name: llama3_json

- weights:
    - Qwen/Qwen2.5-Coder-32B-Instruct
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      override_tt_config:
        trace_region_size: 56000000
    - device: GALAXY_T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        TT_MM_THROTTLE_PERF: 5
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/t3k_mesh_graph_descriptor.textproto"
      override_tt_config:
        fabric_config: FABRIC_1D
  status: EXPERIMENTAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    Qwen/Qwen2.5-Coder-32B-Instruct:
      tool_call_parser_name: hermes

# =============================================================================
# Gemma 4 family - GPU reference specs (bring-your-own vLLM server; see
# docs/gpu_workflows.md). Used to collect GPU reference eval scores for
# GPQA-Diamond, Terminal-Bench 2.0, and SWE-Bench Verified. These are dev-only
# entries; the dev catalog does not require tt_metal_commit/version pins (the
# GPU bring-your-own-server path doesn't build a tt-metal docker image).
# =============================================================================
- weights:
    - google/gemma-4-26B-A4B-it
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    # Matches vLLM nightly [BH-QB-2] Gemma4-26B-A4B (P150x4, max_model_len 128k).
    # Nightly uses max_num_seqs=1 + TT_LEAN_DECODE_WARMUP + 300MB. Serving
    # max_concurrency=32 needs a larger trace region even with lean warmup.
    - device: P300X2
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        MESH_DEVICE: "P150x4"
        GEMMA4_CCL_TOPOLOGY: "linear"
        GEMMA4_PAGE_BLOCK_SIZE: "64"
        GEMMA4_MAX_TOKENS_ALL_USERS: "131072"
        GEMMA4_VLLM_SINGLE_CHUNK: "0"
        # Off by default: traced multi-chunk sp1 alone needs ~1.08GB on 26B.
        # Enable with GEMMA4_CHUNKED_PREFILL_TRACE=1 once trace_region covers it.
        GEMMA4_CHUNKED_PREFILL_TRACE: "0"
      override_tt_config:
        fabric_config: FABRIC_1D
        # B=1+B=32 lean decode; nightly uses 300MB + max_num_seqs=1.
        trace_region_size: 1200000000
        sample_on_device_mode: decode_only
      vllm_args:
        # Overlap host schedule with device decode (metal supports_async_decode; #51186).
        async_scheduling: true
        enable-auto-tool-choice: true
        tool-call-parser: gemma4
        default-chat-template-kwargs: '{"enable_thinking": true}'
        reasoning-parser: gemma4
        enable_chunked_prefill: true
        max_num_batched_tokens: 4096
        long_prefill_token_threshold: 4096
    - device: P150X8
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        MESH_DEVICE: "P150x8"
        GEMMA4_PAGE_BLOCK_SIZE: "64"
        GEMMA4_MAX_TOKENS_ALL_USERS: "131072"
        GEMMA4_VLLM_SINGLE_CHUNK: "0"
        GEMMA4_CHUNKED_PREFILL_TRACE: "0"
      override_tt_config:
        fabric_config: FABRIC_1D
        trace_region_size: 1200000000
        sample_on_device_mode: decode_only
      vllm_args:
        # Overlap host schedule with device decode (metal supports_async_decode; #51186).
        async_scheduling: true
        enable-auto-tool-choice: true
        tool-call-parser: gemma4
        default-chat-template-kwargs: '{"enable_thinking": true}'
        reasoning-parser: gemma4
        enable_chunked_prefill: true
        max_num_batched_tokens: 4096
        long_prefill_token_threshold: 4096
    # WH T3K (1x8 = 8x n300, ~96 GB). Metal policy (generator_trace 26B-A4B/T3K):
    # bounded + multi-chunk (2048) above 16k. 4k / 32k / 128k all PASS, but MoE
    # prefill on Wormhole is far slower than the dense 31B — TTFT 60 s @4k,
    # 449 s @32k and 1506 s @128k (vs 31B's 55 s @32k). Serve 32k: 128k is
    # functional but a ~25 min TTFT is not a usable release target.
    - device: T3K
      # Decode batch ceiling 32, same as the Blackhole specs. max_context is the
      # whole-device KV pool (it sets both max_model_len and
      # max_tokens_all_users), so raising concurrency partitions that fixed pool
      # rather than multiplying it — 32 concurrent users share max_context
      # tokens. Wormhole's 12 GB per ASIC therefore constrains max_context
      # below, not max_concurrency here.
      max_concurrency: 32
      max_context: 32768  # 32 * 1024 — unbounded sliding (multi-request safe)
      default_impl: true
      env_vars:
        MESH_DEVICE: "T3K"
        # Ring is a Blackhole >=8-device default only; WH must stay Linear
        # (models/demos/gemma4/tt/ccl.py default_ccl_topology).
        GEMMA4_CCL_TOPOLOGY: "linear"
        GEMMA4_PAGE_BLOCK_SIZE: "64"
        GEMMA4_MAX_TOKENS_ALL_USERS: "32768"
        GEMMA4_VLLM_SINGLE_CHUNK: "0"
        GEMMA4_CHUNKED_PREFILL_TRACE: "1"
        # KNOWN BAD ON WORMHOLE — concurrent decode returns nondeterministic
        # garbage. Measured on T3K 12B (distinct arithmetic prompts, temp 0,
        # 3 repeats): every request completes, but correctness swings run to run
        # — async OFF gave 4/4, 0/4, 0/4 at concurrency 4 and 1/8, 8/8, 8/8 at
        # concurrency 8; async ON gave 4/4, 0/4, 0/4 and 0/8, 8/8, 3/8. Only
        # concurrency 1 is stable. So max_concurrency: 32 below is the BH-parity
        # target, NOT a verified-good setting on Wormhole yet.
        #
        # async decode is pinned OFF here as a precaution, not as a proven fix:
        # across those repeats it was never worse than ON and was the only
        # config with a clean 3/3 at concurrency 1, but n=3 cannot separate it
        # from the underlying corruption. The BH async mitigations (async-ahead
        # merge, preempt gen accounting, page-table -1 pad) are BH-validated
        # only. Revisit both this flag and max_concurrency once the concurrent
        # decode corruption is root-caused on WH.
        GEMMA4_SUPPORTS_ASYNC_DECODE: "0"
      override_tt_config:
        fabric_config: FABRIC_1D
        # WH cannot afford the BH 512 MB trace region; 90 MB matches the other
        # T3K LLM specs and the raised gemma4 WH demo default. Note this only
        # fits the default {B=1, B=max} decode-trace warmup — capturing every
        # power-of-two bucket overflows it (mesh_trace.cpp TT_FATAL).
        trace_region_size: 200000000
        # On-device decode sampling is required on a TP mesh so token ids
        # >= 65536 are reachable (host sampling only sees device 0's shard).
        sample_on_device_mode: decode_only
      vllm_args:
        # No async_scheduling on Wormhole. Measured on T3K 12B at max_num_seqs 32:
        # async ON gives 32/32 correct on only 4 of 6 reps (30/32 on the other two,
        # degenerate repetition) at 377-389 tok/s aggregate, while async OFF gives
        # 32/32 on four consecutive reps at 363-383 tok/s. Identical throughput,
        # strictly worse correctness — so it is pure downside here. The platform
        # also disables it automatically once the model reports
        # supports_async_decode=False (GEMMA4_SUPPORTS_ASYNC_DECODE=0 above).
        # Re-enable together with that env once the WH async path is fixed.
        enable-auto-tool-choice: true
        tool-call-parser: gemma4
        default-chat-template-kwargs: '{"enable_thinking": true}'
        reasoning-parser: gemma4
        # Token-chunked prefill (tenstorrent/vllm#448). Must equal the chunk the
        # metal policy resolves for this (model, device) — 2048 on every WH board.
        enable_chunked_prefill: true
        max_num_batched_tokens: 2048
        long_prefill_token_threshold: 2048
    - device: GPU
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
  status: EXPERIMENTAL
  has_builtin_warmup: true
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    google/gemma-4-26B-A4B-it:
      reasoning_parser_name: gemma4
      tool_call_parser_name: gemma4

- weights:
    - google/gemma-4-E4B-it
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: GPU
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
  status: EXPERIMENTAL
  metadata:
    google/gemma-4-E4B-it:
      reasoning_parser_name: gemma4
      tool_call_parser_name: gemma4

- weights:
    - google/gemma-4-E2B-it
  impl: tt_transformers
  inference_engine: VLLM
  device_model_specs:
    - device: GPU
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
  status: EXPERIMENTAL
  metadata:
    google/gemma-4-E2B-it:
      reasoning_parser_name: gemma4
      tool_call_parser_name: gemma4

# Remote-served model: evaluated against an external OpenAI-compatible
# endpoint via `--server-url` (e.g. Tenstorrent console). The device entry
# is a placeholder so the model resolves through MODEL_SPECS / EVAL_CONFIGS;
# no local TT serving is implied.
- weights:
    - moonshotai/Kimi-K2.6
  impl: tt_transformers
  inference_engine: MEDIA
  device_model_specs:
    - device: SUPER_CLUSTER
      max_concurrency: 64
      max_context: 262144  # 256 * 1024;
      default_impl: true
  status: EXPERIMENTAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    moonshotai/Kimi-K2.6:
      remote_api_model: moonshotai/Kimi-K2.6
      reasoning_parser_name: kimi_k2
      tool_call_parser_name: kimi_k2
      # Kimi ships a custom tokenizer; AIPerf-driven sweeps (prefix-cache,
      # spec-decode, agentic-traces) must trust remote code to load it.
      tokenizer_trust_remote_code: true

# Remote-served model: evaluated against an external OpenAI-compatible
# endpoint via `--server-url` (e.g. Tenstorrent console). The  device entry
# is a placeholder so the model resolves through MODEL_SPECS / EVAL_CONFIGS;
# no local TT serving is implied.
- weights:
    - MiniMaxAI/MiniMax-M2.7
  impl: tt_transformers
  inference_engine: MEDIA
  device_model_specs:
    - device: SUPER_CLUSTER
      max_concurrency: 64
      max_context: 204800  # 200 * 1024;
      default_impl: true
  status: EXPERIMENTAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    MiniMaxAI/MiniMax-M2.7:
      remote_api_model: MiniMaxAI/MiniMax-M2.7
      reasoning_parser_name: minimax_m2
      tool_call_parser_name: minimax_m2

- weights:
    - openai/gpt-oss-120b
  impl: gpt_oss
  inference_engine: MEDIA
  device_model_specs:
    - device: SUPER_CLUSTER
      max_concurrency: 38
      max_context: 131072  # 128 * 1024
      default_impl: true
  status: EXPERIMENTAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    openai/gpt-oss-120b:
      remote_api_model: openai/gpt-oss-120b
      reasoning_parser_name: openai_gptoss
      tool_call_parser_name: openai

- weights:
    - moonshotai/Kimi-K2.7-Code
  impl: tt_transformers
  inference_engine: MEDIA
  device_model_specs:
    - device: SUPER_CLUSTER
      max_concurrency: 64
      max_context: 256000  # 256 * 1000;
      default_impl: true
  status: FUNCTIONAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    moonshotai/Kimi-K2.7-Code:
      remote_api_model: moonshotai/Kimi-K2.7-Code
      reasoning_parser_name: kimi_k2
      tool_call_parser_name: kimi_k2
      # Kimi ships a custom tokenizer; AIPerf-driven sweeps (prefix-cache,
      # spec-decode, agentic-traces) must trust remote code to load it.
      tokenizer_trust_remote_code: true

# Remote-served model: evaluated against an external OpenAI-compatible
# endpoint via `--server-url`. The device entry is a placeholder so the model
# resolves through MODEL_SPECS / EVAL_CONFIGS; no local TT serving is implied.
- weights:
    - moonshotai/Kimi-K3
  impl: tt_transformers
  inference_engine: MEDIA
  device_model_specs:
    - device: SUPER_CLUSTER
      max_concurrency: 93
      max_context: 1048576  # 1024 * 1024;
      default_impl: true
  status: EXPERIMENTAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    moonshotai/Kimi-K3:
      remote_api_model: moonshotai/Kimi-K3
      # K3 ships its own parsers, distinct from the kimi_k2 pair used by
      # K2.6/K2.7-Code: https://recipes.vllm.ai/moonshotai/Kimi-K3
      reasoning_parser_name: kimi_k3
      tool_call_parser_name: kimi_k3
      # Kimi ships a custom tokenizer; AIPerf-driven sweeps (prefix-cache,
      # spec-decode, agentic-traces) must trust remote code to load it.
      tokenizer_trust_remote_code: true

- weights:
    - MiniMaxAI/MiniMax-M3
  impl: tt_transformers
  inference_engine: MEDIA
  device_model_specs:
    - device: SUPER_CLUSTER
      max_concurrency: 64
      max_context: 1048576  # 1024 * 1024;
      default_impl: true
  status: EXPERIMENTAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    MiniMaxAI/MiniMax-M3:
      remote_api_model: MiniMaxAI/MiniMax-M3
      reasoning_parser_name: minimax_m3
      tool_call_parser_name: minimax_m3

- weights:
    - Qwen/Qwen3-4B
  impl: forge_vllm_plugin
  min_disk_gb: 15
  min_ram_gb: 8
  model_type: LLM
  inference_engine: FORGE
  uses_tensor_model_cache: false
  device_model_specs:
    - device: P150
      max_concurrency: 32
      max_context: 40960  # Qwen3 native max_position_embeddings
      default_impl: true
      eval_max_retries: 1
      env_vars:
        MAX_MODEL_LENGTH: "40960"
        MAX_NUM_SEQS: "32"
        GPU_MEMORY_UTILIZATION: "0.5"
        TT_KV_POOL_GB: "32"
        OPTIMIZATION_LEVEL: "1"
        CPU_SAMPLING: "false"
        ENABLE_TRACE: "true"
        KV_CACHE_DTYPE: "bfp_bf8"
        PREFILL_CHUNK_SIZE: "1024"
        MIN_NUM_SEQS: "1"  # b1-prefill: compile [1,n] graph alongside [32,n] (tt-xla #5281)
        PREFILL_BATCH_THRESHOLD: "16"  # route <=16 pending prefills to b1, serially

- weights:
    - meta-llama/Llama-3.2-3B
    - meta-llama/Llama-3.2-3B-Instruct
  model_display_name: Llama-3.2-3B
  impl: forge_vllm_plugin
  min_disk_gb: 15
  min_ram_gb: 8
  model_type: LLM
  inference_engine: FORGE
  uses_tensor_model_cache: false
  device_model_specs:
    - device: P150
      max_concurrency: 32
      max_context: 65536  # 64 * 1024
      default_impl: true
      eval_max_retries: 1
      env_vars:
        MAX_MODEL_LENGTH: "65536"
        MAX_NUM_SEQS: "32"
        GPU_MEMORY_UTILIZATION: "0.5"
        TT_KV_POOL_GB: "32"
        OPTIMIZATION_LEVEL: "1"
        CPU_SAMPLING: "false"
        ENABLE_TRACE: "true"
        KV_CACHE_DTYPE: "bfp_bf8"
        PREFILL_CHUNK_SIZE: "1024"
        MIN_NUM_SEQS: "1"  # b1-prefill: compile [1,n] graph alongside [32,n] (tt-xla #5281)
        PREFILL_BATCH_THRESHOLD: "16"  # route <=16 pending prefills to b1, serially

- weights:
    - meta-llama/Llama-3.1-8B-Instruct
  impl: forge_vllm_plugin
  min_disk_gb: 20
  min_ram_gb: 12
  model_type: LLM
  inference_engine: FORGE
  uses_tensor_model_cache: false
  device_model_specs:
    - device: P150
      max_concurrency: 32
      max_context: 65536  # 64 * 1024
      default_impl: true
      eval_max_retries: 1
      env_vars:
        MAX_MODEL_LENGTH: "65536"
        MAX_NUM_SEQS: "32"
        GPU_MEMORY_UTILIZATION: "0.35"
        TT_KV_POOL_GB: "32"
        OPTIMIZATION_LEVEL: "1"
        CPU_SAMPLING: "false"
        ENABLE_TRACE: "true"
        KV_CACHE_DTYPE: "bfp_bf8"
        PREFILL_CHUNK_SIZE: "1024"
        MIN_NUM_SEQS: "1"  # b1-prefill: compile [1,n] graph alongside [32,n] (tt-xla #5281)
        PREFILL_BATCH_THRESHOLD: "16"  # route <=16 pending prefills to b1, serially

- weights:
    - Qwen/Qwen3-8B
  impl: forge_vllm_plugin
  min_disk_gb: 20
  min_ram_gb: 12
  model_type: LLM
  inference_engine: FORGE
  uses_tensor_model_cache: false
  device_model_specs:
    - device: P150
      max_concurrency: 32
      max_context: 40960  # Qwen3 native max_position_embeddings
      default_impl: true
      eval_max_retries: 1
      env_vars:
        MAX_MODEL_LENGTH: "40960"
        MAX_NUM_SEQS: "32"
        GPU_MEMORY_UTILIZATION: "0.35"
        TT_KV_POOL_GB: "32"
        OPTIMIZATION_LEVEL: "1"
        CPU_SAMPLING: "false"
        ENABLE_TRACE: "true"
        KV_CACHE_DTYPE: "bfp_bf8"
        PREFILL_CHUNK_SIZE: "1024"
        MIN_NUM_SEQS: "1"  # b1-prefill: compile [1,n] graph alongside [32,n] (tt-xla #5281)
        PREFILL_BATCH_THRESHOLD: "16"  # route <=16 pending prefills to b1, serially

- weights:
    - tiiuae/Falcon3-7B-Instruct
  impl: forge_vllm_plugin
  min_disk_gb: 20
  min_ram_gb: 12
  model_type: LLM
  inference_engine: FORGE
  uses_tensor_model_cache: false
  status: EXPERIMENTAL
  device_model_specs:
    - device: P150
      max_concurrency: 32
      max_context: 32768  # Falcon3 native max_position_embeddings
      default_impl: true
      eval_max_retries: 1
      env_vars:
        MAX_MODEL_LENGTH: "32768"
        MAX_NUM_SEQS: "32"
        GPU_MEMORY_UTILIZATION: "0.35"
        TT_KV_POOL_GB: "32"
        OPTIMIZATION_LEVEL: "1"
        CPU_SAMPLING: "false"
        ENABLE_TRACE: "true"
        KV_CACHE_DTYPE: "bfp_bf8"
        # 1024 (not 2048): Falcon3 head_dim=256 makes the b32 prefill SDPA buffer
        # DRAM-OOM at chunk 2048 during trace-capture.
        PREFILL_CHUNK_SIZE: "1024"
        MIN_NUM_SEQS: "1"  # b1-prefill: compile [1,n] graph alongside [32,n] (tt-xla #5281)
        PREFILL_BATCH_THRESHOLD: "16"  # route <=16 pending prefills to b1, serially

- weights:
    - tiiuae/Falcon3-7B-Instruct
  impl: tt_transformers
  min_disk_gb: 20
  min_ram_gb: 12
  model_type: LLM
  inference_engine: VLLM
  uses_tensor_model_cache: false
  status: EXPERIMENTAL
  device_model_specs:
    - device: GPU
      max_concurrency: 32
      max_context: 32768  # Falcon3 native max_position_embeddings
      default_impl: true

- weights:
    # gemma-4-31b-it Forge LLM (tensor-parallel) on p300x2/QB2. Lowercase
    # basename matches tt-media-server ModelNames.GEMMA_4_31B_IT. batch-16 / 4K
    # is the best fitting balance (~76 tok/s aggregate). Dims are env-driven here
    # (constants.py only carries the TP mesh topology), mirroring the single-chip
    # forge LLMs above.
    - google/gemma-4-31b-it
  impl: forge_vllm_plugin
  min_disk_gb: 80
  min_ram_gb: 32
  model_type: LLM
  inference_engine: FORGE
  uses_tensor_model_cache: false
  status: EXPERIMENTAL
  device_model_specs:
    - device: P300X2
      max_concurrency: 16
      max_context: 4096
      # gemma-4-31b-it on P300X2 also has a tt_transformers/vLLM spec (llm.yaml,
      # this model's Model Readiness path); that one is the default impl. Both
      # can't be default_impl or determine_server_type raises "Multiple default
      # impls". This EXPERIMENTAL forge path stays selectable via explicit impl.
      default_impl: false
      eval_max_retries: 1
      env_vars:
        MAX_MODEL_LENGTH: "4096"
        MAX_NUM_SEQS: "16"
        GPU_MEMORY_UTILIZATION: "0.9"
        OPTIMIZATION_LEVEL: "0"
        ENABLE_TRACE: "true"
        CPU_SAMPLING: "false"
        # 4-chip (1,4) TP mesh: the runner doesn't auto-set this and the image
        # maps p300x2->p150, so pin the p300_x2 descriptor (else stablehlo
        # "unknown mesh: @mesh" at compile). -e overrides --env-file here.
        TT_MESH_GRAPH_DESC_PATH: "/home/container_app_user/app/server/venv-worker/lib/python3.12/site-packages/pjrt_plugin_tt/tt-metal/tt_metal/fabric/mesh_graph_descriptors/p300_x2_mesh_graph_descriptor.textproto"

- weights:
    # Qwen3-32B Forge LLM (tensor-parallel) on p300x2/QB2, mirroring gemma-4-31b.
    # basename "Qwen3-32B" matches ModelNames.QWEN_3_32B. batch-16 / 4K (same as
    # gemma; not yet independently swept on Qwen -- local verify is the check).
    - Qwen/Qwen3-32B
  impl: forge_vllm_plugin
  min_disk_gb: 80
  min_ram_gb: 32
  model_type: LLM
  inference_engine: FORGE
  uses_tensor_model_cache: false
  status: EXPERIMENTAL
  device_model_specs:
    - device: P300X2
      max_concurrency: 16
      max_context: 4096
      default_impl: true
      eval_max_retries: 1
      env_vars:
        MAX_MODEL_LENGTH: "4096"
        MAX_NUM_SEQS: "16"
        GPU_MEMORY_UTILIZATION: "0.9"
        OPTIMIZATION_LEVEL: "0"
        ENABLE_TRACE: "true"
        CPU_SAMPLING: "false"
        TT_MESH_GRAPH_DESC_PATH: "/home/container_app_user/app/server/venv-worker/lib/python3.12/site-packages/pjrt_plugin_tt/tt-metal/tt_metal/fabric/mesh_graph_descriptors/p300_x2_mesh_graph_descriptor.textproto"

- weights:
    # Mistral-Small-3.1-24B-Instruct-2503 Forge LLM (tensor-parallel) on blackhole galaxy,
    # mirroring gemma-4-31b above. basename matches tt-media-server
    # ModelNames.MISTRAL_SMALL_3_1_24B_INSTRUCT_2503. Pixtral-based multimodal
    # model served text-only (runner zeros limit_mm_per_prompt). Config mirrors
    # the tt-xla vLLM benchmark: b32 / 8K context / bfp8 weights+KV.
    - mistralai/Mistral-Small-3.1-24B-Instruct-2503
  impl: forge_vllm_plugin
  min_disk_gb: 80
  min_ram_gb: 32
  model_type: LLM
  inference_engine: FORGE
  uses_tensor_model_cache: false
  status: EXPERIMENTAL
  device_model_specs:
    - device: BLACKHOLE_GALAXY
      max_concurrency: 32  # 8 * 4
      max_context: 8192
      default_impl: true
      eval_max_retries: 1
      known_issues:
        - workflow_type: EVALS
          task_name: gpqa_diamond_cot_zeroshot
          reason: "35.86% vs published 45.96% (ratio 0.78, needs >=43.66%), cot_zeroshot methodology differs from the publisher's. Not a regression signal until gpu_reference_score is populated."
        - workflow_type: EVALS
          task_name: humaneval_instruct
          reason: "79.27% vs published 88.41% (ratio 0.90, needs >=83.99%). Not a regression signal until gpu_reference_score is populated."
      env_vars:
        MAX_MODEL_LENGTH: "8192"
        MAX_NUM_SEQS: "32"
        GPU_MEMORY_UTILIZATION: "0.65"
        OPTIMIZATION_LEVEL: "1"
        ENABLE_TRACE: "true"
        CPU_SAMPLING: "true"
        TT_MESH_GRAPH_DESC_PATH: "/home/container_app_user/app/server/venv-worker/lib/python3.12/site-packages/pjrt_plugin_tt/tt-metal/tt_metal/fabric/mesh_graph_descriptors/single_bh_galaxy_mesh_graph_descriptor.textproto"

- weights:
    - meta-llama/Llama-3.1-70B
  impl: forge_vllm_plugin
  min_disk_gb: 150
  min_ram_gb: 80
  model_type: LLM
  inference_engine: MEDIA
  status: EXPERIMENTAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  device_model_specs:
    - device: T3K
      max_concurrency: 1
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "32768"
        VLLM__MAX_MODEL_LENGTH: "131072"
        VLLM__MIN_CONTEXT_LENGTH: "1024"

- weights:
    - zai-org/GLM-5.2
  impl: tt_transformers
  inference_engine: MEDIA
  device_model_specs:
    - device: SUPER_CLUSTER
      max_concurrency: 64
      max_context: 1048576  # 1024 * 1024;
      default_impl: true
  status: FUNCTIONAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    zai-org/GLM-5.2:
      remote_api_model: zai-org/GLM-5.2
      reasoning_parser_name: glm45
      tool_call_parser_name: glm47

# Remote-served model: evaluated against an external OpenAI-compatible
# endpoint via `--server-url`. Same base as GLM-5.2; gains are post-training.
# The device entry is a placeholder so the model resolves through MODEL_SPECS
# / EVAL_CONFIGS; no local TT serving is implied.
- weights:
    - zai-org/GLM-5.3
  impl: tt_transformers
  inference_engine: MEDIA
  device_model_specs:
    - device: SUPER_CLUSTER
      max_concurrency: 80
      max_context: 1048576  # 1024 * 1024;
      default_impl: true
  status: EXPERIMENTAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  metadata:
    zai-org/GLM-5.3:
      remote_api_model: zai-org/GLM-5.3
      reasoning_parser_name: glm45
      tool_call_parser_name: glm47

- weights:
    - Qwen/Qwen3.5-27B
  impl: qwen36_blackhole  # the same model structure as 3.6
  inference_engine: VLLM
  model_type: LLM
  supported_modalities:
    - text
  device_model_specs:
    - device: P150X4
      max_concurrency: 32
      max_context: 262144
      max_tokens_all_users_override: 525312
      default_impl: true
      env_vars:
        TT_QWEN35_TEXT_VER: qwen36_blackhole
        HF_MODEL: Qwen/Qwen3.5-27B
        MESH_DEVICE: "P150x4"
        QWEN36_MAX_TOKENS_ALL_USERS: "525312"
      vllm_args:
        enable_auto_tool_choice: true
        reasoning_parser: qwen3
        tool_call_parser: qwen3_coder
      override_tt_config:
        fabric_config: FABRIC_1D
        trace_region_size: 1073741824
        l1_small_size: 24576
        sample_on_device_mode: decode_only
  status: EXPERIMENTAL
  has_builtin_warmup: true
  metadata:
    Qwen/Qwen3.5-27B:
      reasoning_parser_name: qwen3
      tool_call_parser_name: qwen3_coder

# ---------------------------------------------------------------------------
# Quetzal-generated (compiled) serve lane — meta-llama/Llama-3.2-1B-Instruct,
# QB2 / P300X2 (four Blackhole chips driven as a P150x4 mesh).
#
# GENERATED, not hand-authored: emit this block with
#   scripts/generate_quetzal_model_row.py \
#       --catalog <tt-quetzalcoatlus>/serving/catalog/dev_llm_quetzal.yaml \
#       --package-id sha256-...-... --model meta-llama/Llama-3.2-1B-Instruct
# rather than editing the paths by hand — every QZ_/QUETZAL_ path is derived
# from the one package id, so a stale copy is the classic mis-serve.
#
# All package paths are content-addressed under the package id. On a container
# serve the package is bind-mounted read-only at QUETZAL_PACKAGE_ROOT; on a
# bare Exabox node it is staged/admitted by content hash (verify_bundle +
# QUETZAL_BUNDLE_MANIFEST_SHA256) — see docs/quetzal_bare_node_serve.md.
#
# default_impl is false: upstream already ships a native tt-metal impl for this
# pair, so a Quetzal run MUST pass `--impl quetzal`; omission keeps the native
# default.
- weights:
    - meta-llama/Llama-3.2-1B-Instruct
  impl: quetzal
  inference_engine: VLLM
  model_type: LLM
  supported_modalities:
    - text
  device_model_specs:
    - device: P300X2
      max_concurrency: 1
      max_context: 4096
      default_impl: false
      env_vars:
        ARCH_NAME: blackhole
        MESH_DEVICE: P150x4
        QUETZAL_VLLM: "1"
        QUETZAL_MODEL: meta-llama/Llama-3.2-1B-Instruct
        QUETZAL_HF_REVISION: 9213176726f574b556790deb65791e0c5aa438b6
        # Pin the Quetzal source that emitted this package. The serve-required
        # KV-cache-key patch (e2e_pcc.discover_cache_keys `cache_layers_<N>_keys`)
        # lives IN the Quetzal package at this commit — referenced, not vendored.
        QUETZAL_REQUIRED_SOURCE_REVISION: efa39286d1b94350ad97cba814b66d8601897524
        TT_QUETZAL_COMMIT_SHA: efa39286d1b94350ad97cba814b66d8601897524
        QUETZAL_REQUIRED_TT_METAL_COMMIT: 1c2aff5064f19936d70127c80fe9333d687cc427
        TT_METAL_COMMIT_SHA_OR_TAG: 1c2aff5064f19936d70127c80fe9333d687cc427
        QUETZAL_PACKAGE_ID: sha256-189fd3bbb381699ec59223d227e4b72854d8e0521db58d621f06866718bb9879-2e154513ea47ccc8cfde7b4512a258ef02c78ac0283115d2a6aaacb6f05d8f8f
        QUETZAL_BUNDLE_MANIFEST_SHA256: c6a1426cfa7ff599d5b6271301a415544ae3ac0e7b4e4c1fdb5a5e4d1441c422
        QUETZAL_PACKAGE_ROOT: /home/container_app_user/cache_root/quetzal/packages/sha256-189fd3bbb381699ec59223d227e4b72854d8e0521db58d621f06866718bb9879-2e154513ea47ccc8cfde7b4512a258ef02c78ac0283115d2a6aaacb6f05d8f8f
        QZ_MODELS_ROOT: /home/container_app_user/cache_root/quetzal/packages/sha256-189fd3bbb381699ec59223d227e4b72854d8e0521db58d621f06866718bb9879-2e154513ea47ccc8cfde7b4512a258ef02c78ac0283115d2a6aaacb6f05d8f8f
        QZ_QUALIFICATION_MANIFEST: /home/container_app_user/cache_root/quetzal/packages/sha256-189fd3bbb381699ec59223d227e4b72854d8e0521db58d621f06866718bb9879-2e154513ea47ccc8cfde7b4512a258ef02c78ac0283115d2a6aaacb6f05d8f8f/qualification_manifest.yaml
        QUETZAL_PREFILL_GENERATED_PY: /home/container_app_user/cache_root/quetzal/packages/sha256-189fd3bbb381699ec59223d227e4b72854d8e0521db58d621f06866718bb9879-2e154513ea47ccc8cfde7b4512a258ef02c78ac0283115d2a6aaacb6f05d8f8f/compiled/meta-llama_Llama-3.2-1B-Instruct/full/prefill/generated.py
        QUETZAL_DECODE_GENERATED_PY: /home/container_app_user/cache_root/quetzal/packages/sha256-189fd3bbb381699ec59223d227e4b72854d8e0521db58d621f06866718bb9879-2e154513ea47ccc8cfde7b4512a258ef02c78ac0283115d2a6aaacb6f05d8f8f/compiled/meta-llama_Llama-3.2-1B-Instruct/full/decode/generated.py
        QUETZAL_PREFILL_METADATA_JSON: /home/container_app_user/cache_root/quetzal/packages/sha256-189fd3bbb381699ec59223d227e4b72854d8e0521db58d621f06866718bb9879-2e154513ea47ccc8cfde7b4512a258ef02c78ac0283115d2a6aaacb6f05d8f8f/compiled/meta-llama_Llama-3.2-1B-Instruct/full/prefill/metadata.json
        QUETZAL_DECODE_METADATA_JSON: /home/container_app_user/cache_root/quetzal/packages/sha256-189fd3bbb381699ec59223d227e4b72854d8e0521db58d621f06866718bb9879-2e154513ea47ccc8cfde7b4512a258ef02c78ac0283115d2a6aaacb6f05d8f8f/compiled/meta-llama_Llama-3.2-1B-Instruct/full/decode/metadata.json
        QUETZAL_WEIGHTS: /home/container_app_user/cache_root/quetzal/packages/sha256-189fd3bbb381699ec59223d227e4b72854d8e0521db58d621f06866718bb9879-2e154513ea47ccc8cfde7b4512a258ef02c78ac0283115d2a6aaacb6f05d8f8f/compiled_weights/meta-llama_Llama-3.2-1B-Instruct/full/weights.pt
        QZ_MMAP_WEIGHTS: "1"
        # Weights + tokenizer resolve offline from the package / HF cache.
        HF_HUB_OFFLINE: "1"
        TRANSFORMERS_OFFLINE: "1"
        # Load the TT platform and the generated registry hook only; the
        # handcrafted tt_model_registry is excluded so it cannot compete for the
        # architecture alias.
        VLLM_PLUGINS: quetzal_model_registry,tt
        TT_VLLM_BUILTIN_MODELS: "0"
      vllm_args:
        block_size: 64
        max_model_len: 4096
        max_num_seqs: 1
        revision: 9213176726f574b556790deb65791e0c5aa438b6
        tokenizer_revision: 9213176726f574b556790deb65791e0c5aa438b6
        # Tool calling for SWE/agentic. vLLM requires --tool-call-parser
        # alongside --enable-auto-tool-choice; llama3_json is the built-in parser
        # matching the catalog metadata tool_call_parser_name. Omitting it is the
        # documented landmine that silently voids agentic/SWE scores.
        enable-auto-tool-choice: true
        tool-call-parser: llama3_json
      override_tt_config:
        fabric_config: FABRIC_1D
        l1_small_size: 16384
        trace_region_size: 90000000
  status: EXPERIMENTAL
  has_builtin_warmup: true
  metadata:
    meta-llama/Llama-3.2-1B-Instruct:
      tool_call_parser_name: llama3_json
