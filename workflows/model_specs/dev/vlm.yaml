# Model spec catalog: VLM templates.
# Edit this file to add or modify a VLM model spec.
# Schema: workflows/model_spec.py (ModelSpecTemplate, DeviceModelSpec).
templates:
# =============================================================================
# VLM templates
# =============================================================================
- weights:
    - google/gemma-3-4b-it
    - google/medgemma-4b-it
  impl: tt_transformers
  version: "0.9.0"
  tt_metal_commit: aecd1d7
  vllm_commit: 0da90eb
  inference_engine: VLLM
  device_model_specs:
    - device: N150
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      vllm_args:
        limit-mm-per-prompt: '{"image": 10}'
        mm-processor-cache-gb: 0
      override_tt_config:
        l1_small_size: 4096
        fabric_config: FABRIC_1D
    - device: N300
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      vllm_args:
        limit-mm-per-prompt: '{"image": 10}'
        mm-processor-cache-gb: 0
      override_tt_config:
        l1_small_size: 4096
        fabric_config: FABRIC_1D
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      vllm_args:
        limit-mm-per-prompt: '{"image": 10}'
        data_parallel_size: 8
        mm-processor-cache-gb: 0
      override_tt_config:
        l1_small_size: 4096
        fabric_config: FABRIC_1D
    - device: P150
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      vllm_args:
        limit-mm-per-prompt: '{"image": 10}'
        mm-processor-cache-gb: 0
      override_tt_config:
        l1_small_size: 4096
        fabric_config: FABRIC_1D
  model_type: VLM
  status: EXPERIMENTAL
  supported_modalities:
    - text
    - image
  has_builtin_warmup: true

- weights:
    - google/gemma-3-27b-it
    - google/medgemma-27b-it
  impl: tt_transformers
  version: "0.9.0"
  tt_metal_commit: aecd1d7
  vllm_commit: 0da90eb
  inference_engine: VLLM
  device_model_specs:
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      vllm_args:
        limit-mm-per-prompt: '{"image": 10}'
        mm-processor-cache-gb: 0
      override_tt_config:
        l1_small_size: 4096
        fabric_config: FABRIC_1D
        sample_on_device_mode: decode_only
    - device: GALAXY_T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        TT_MM_THROTTLE_PERF: 5
        TT_MESH_GRAPH_DESC_PATH: "../../tt-metal/tt_metal/fabric/mesh_graph_descriptors/t3k_mesh_graph_descriptor.textproto"
      vllm_args:
        limit-mm-per-prompt: '{"image": 10}'
        mm-processor-cache-gb: 0
      override_tt_config:
        l1_small_size: 4096
        fabric_config: FABRIC_1D
        sample_on_device_mode: decode_only
    - device: GALAXY
      max_concurrency: 32  # 8 * 4
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        TT_MM_THROTTLE_PERF: 5
      vllm_args:
        limit-mm-per-prompt: '{"image": 10}'
        data_parallel_size: 4
        mm-processor-cache-gb: 0
      override_tt_config:
        l1_small_size: 4096
        fabric_config: FABRIC_1D_RING
        sample_on_device_mode: decode_only
    - device: P300X2
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      vllm_args:
        limit-mm-per-prompt: '{"image": 10}'
        mm-processor-cache-gb: 0
      override_tt_config:
        l1_small_size: 4096
        fabric_config: FABRIC_1D
        sample_on_device_mode: decode_only
        trace_region_size: 402653184  # 384 * 1024 * 1024
      system_requirements:
        firmware:
          specifier: ">=19.2.0"
          mode: STRICT
        kmd:
          specifier: ">=2.5.0"
          mode: STRICT
  model_type: VLM
  status: EXPERIMENTAL
  supported_modalities:
    - text
    - image
  has_builtin_warmup: true

- weights:
    - Qwen/Qwen3-VL-32B-Instruct
  impl: tt_transformers
  inference_engine: VLLM
  model_type: VLM
  version: "0.14.0"
  tt_metal_commit: 80180b9
  vllm_commit: 7678b70
  device_model_specs:
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      vllm_args:
        mm-processor-cache-gb: 0
  status: FUNCTIONAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  supported_modalities:
    - text
    - image

- weights:
    - Qwen/Qwen2.5-VL-3B-Instruct
  impl: tt_transformers
  version: "0.9.0"
  tt_metal_commit: c18569e
  vllm_commit: b2894d3
  inference_engine: VLLM
  model_type: VLM
  device_model_specs:
    - device: N150
      max_concurrency: 32
      max_context: 32768  # 32 * 1024
      default_impl: true
    - device: N300
      max_concurrency: 32
      max_context: 32768  # 32 * 1024
      default_impl: true
  status: EXPERIMENTAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  supported_modalities:
    - text
    - image
  metadata:
    Qwen/Qwen2.5-VL-3B-Instruct:
      tool_call_parser_name: hermes

- weights:
    - Qwen/Qwen2.5-VL-7B-Instruct
  impl: tt_transformers
  version: "0.9.0"
  tt_metal_commit: c18569e
  vllm_commit: b2894d3
  inference_engine: VLLM
  model_type: VLM
  device_model_specs:
    - device: N300
      max_concurrency: 32
      max_context: 32768  # 32 * 1024
      default_impl: true
      vllm_args:
        mm-processor-cache-gb: 0
      override_tt_config:
        trace_region_size: 10000000
  status: EXPERIMENTAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  supported_modalities:
    - text
    - image
  metadata:
    Qwen/Qwen2.5-VL-7B-Instruct:
      tool_call_parser_name: hermes

- weights:
    - Qwen/Qwen2.5-VL-32B-Instruct
  impl: tt_transformers
  version: "0.9.0"
  tt_metal_commit: c18569e
  vllm_commit: b2894d3
  inference_engine: VLLM
  model_type: VLM
  device_model_specs:
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      vllm_args:
        mm-processor-cache-gb: 0
  status: EXPERIMENTAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  supported_modalities:
    - text
    - image
  metadata:
    Qwen/Qwen2.5-VL-32B-Instruct:
      tool_call_parser_name: hermes

- weights:
    - Qwen/Qwen2.5-VL-72B-Instruct
  impl: tt_transformers
  version: "0.9.0"
  tt_metal_commit: c18569e
  vllm_commit: b2894d3
  inference_engine: VLLM
  model_type: VLM
  device_model_specs:
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      vllm_args:
        mm-processor-cache-gb: 0
      override_tt_config:
        trace_region_size: 28467200
    - device: GPU
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
  status: FUNCTIONAL
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  supported_modalities:
    - text
    - image
  metadata:
    Qwen/Qwen2.5-VL-72B-Instruct:
      tool_call_parser_name: hermes

- weights:
    - meta-llama/Llama-3.2-11B-Vision
    - meta-llama/Llama-3.2-11B-Vision-Instruct
  impl: tt_transformers
  version: "0.9.0"
  tt_metal_commit: v0.61.1-rc1
  vllm_commit: 5cbc982
  inference_engine: VLLM
  device_model_specs:
    - device: N300
      max_concurrency: 16
      max_context: 131072  # 128 * 1024
      default_impl: true
    - device: T3K
      max_concurrency: 16
      max_context: 131072  # 128 * 1024
      default_impl: true
  model_type: VLM
  status: FUNCTIONAL
  supported_modalities:
    - text
    - image

- weights:
    - meta-llama/Llama-3.2-90B-Vision
    - meta-llama/Llama-3.2-90B-Vision-Instruct
  impl: tt_transformers
  version: "0.9.0"
  tt_metal_commit: v0.61.1-rc1
  vllm_commit: 5cbc982
  inference_engine: VLLM
  device_model_specs:
    - device: T3K
      max_concurrency: 32
      max_context: 131072  # 128 * 1024
      default_impl: true
      env_vars:
        MAX_PREFILL_CHUNK_SIZE: 16
  model_type: VLM
  status: FUNCTIONAL
  supported_modalities:
    - text
    - image

# =============================================================================
# tt_symbiote-ported VLM templates
# -----------------------------------------------------------------------------
# These models are served via the tt_symbiote library (HuggingFace-shaped TTNN
# package), wrapped by the vLLM Generator adapter in
# vllm-tt-metal/src/tt_symbiote_generators.py. The pinned tt_metal_commit MUST
# match tt_symbiote RUNTIME_PINS for the model's HF architecture; ttnn is
# source-built from that commit and tt_symbiote installed --no-deps on top.
# =============================================================================
- weights:
    - rednote-hilab/dots.ocr
  impl: tt_symbiote
  inference_engine: VLLM
  model_type: VLM
  # NOTE: `version` is the tt-inference-server IMAGE version (gated >= 0.11.0,
  # used in the image tag) — NOT the tt_symbiote library version. Omitted here so
  # it defaults to the current tt-inference-server VERSION. The tt_symbiote pip
  # version lives in `metadata` below and is read by build_docker_images.py.
  # == tt_symbiote RUNTIME_PINS["DotsOCRForCausalLM"]["tt_metal_commit"] ==
  tt_metal_commit: c09f09c35a1a59a428f0e1b5cdaa8fe59fb1b195
  # Tenstorrent vLLM fork commit compatible with the tt-metal commit above.
  # TODO(validate): confirm against a built image; set to the vllm fork commit
  # paired with tt-metal c09f09c3 in tt-metal's install-vllm-tt.sh era.
  vllm_commit: 7678b70
  device_model_specs:
    - device: T3K
      # S0 greedy-engine bridge serves one request at a time (the pipeline's DP
      # batch is same-prompt replication, not vLLM request batching). See
      # tt_inference_server_integration.md §9.2/§9.6.
      max_concurrency: 1
      # tt_symbiote's dots.ocr PREFILL is not chunked, so a single prefill of
      # >=4096 tokens overflows L1 (TT_THROW program.cpp:934 circular-buffer/L1
      # clash). max_context drives BOTH max_model_len and max_num_batched_tokens
      # (workflows/model_spec.py), but the prefill length is set by the request,
      # not by max_context: the validated 1848x1176 grid is (84,132)=11088 patches
      # -> 2772 image tokens, well under 4096. Raising max_context only enlarges
      # the DECODE budget (KV growth, 1 token/step) so a full multi-table page can
      # be transcribed in one response; it does NOT enlarge our prefill. Caveat:
      # this re-exposes the prefill-L1 limit for any request whose prefill exceeds
      # ~4096 tokens (larger images / long text); the real fix is prefill chunking.
      max_context: 12288       # 12 * 1024 (decode headroom for full-page OCR; prefill stays ~2772)
      default_impl: true
      env_vars:
        # tt_symbiote's dots.ocr is laid out data-parallel on T3K and its
        # @run_on_devices(T3K, mesh_shape=(8, 1)) guard requires the live mesh to
        # be (8, 1). vLLM's TT platform opens the mesh from MESH_DEVICE; the
        # default "T3K" maps to (1, 8) (tensor-parallel), so override to the
        # (8, 1) DP layout the model expects. tt-metal/vLLM accept MESH_DEVICE as
        # a literal shape tuple (same convention the TG specs use for "(4, 8)").
        MESH_DEVICE: "(8, 1)"
      vllm_args:
        # dots.ocr ships custom modeling code on the HF Hub, so vLLM's
        # ModelConfig requires trust_remote_code to load it.
        trust-remote-code: true
        mm-processor-cache-gb: 0
        limit-mm-per-prompt: '{"image": 1}'
        max-num-seqs: 1
      override_tt_config:
        # dots.ocr captures large prefill+decode traces; matches the e2e demo's
        # ttnn.open_mesh_device(trace_region_size=200_000_000).
        trace_region_size: 200000000
  status: EXPERIMENTAL
  # Skip tt-inference-server's generic background trace-capture sweep. That sweep
  # (utils/prompt_client.py::DEFAULT_IMAGE_RESOLUTIONS = [(512,512),(512,1024),
  # (1024,512),(1024,1024)]) feeds arbitrary image resolutions to the model. The
  # tt_symbiote dots.ocr vision tower is only tuned for the validated patch grid
  # (84,132)=11088 patches (box 1848x1176); off-grid resolutions select an
  # untuned vision-SDPA program config whose static circular buffers overflow L1
  # (TT_THROW program.cpp:925, >1.5 MB) and crash EngineCore during warmup.
  # Instead, rely on tt_symbiote's lazy trace capture: the vision/decode traces
  # are captured on the FIRST real request for that request's exact geometry —
  # the same path the standalone e2e (examples/e2e/dots_ocr/run_dots_ocr.py)
  # validates. Requests should resize images to the (84,132) grid until the
  # vision tower's SDPA buckets cover arbitrary resolutions.
  has_builtin_warmup: true
  env_vars:
    VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1
  supported_modalities:
    - text
    - image
  metadata:
    rednote-hilab/dots.ocr:
      # tt_symbiote pip version installed into the image (build_docker_images.py
      # reads this; default index is the production PyPI — see the Dockerfile note).
      tt_symbiote_version: "0.1.4"
