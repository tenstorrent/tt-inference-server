# Model spec catalog: Embedding templates.
# Edit this file to add or modify a Embedding model spec.
# Schema: workflows/model_spec.py (ModelSpecTemplate, DeviceModelSpec).
templates:
# =============================================================================
# Embedding templates
# =============================================================================
- weights:
    - BAAI/bge-large-en-v1.5
  impl: tt_vllm_plugin
  min_disk_gb: 15
  min_ram_gb: 6
  model_type: EMBEDDING
  inference_engine: MEDIA
  device_model_specs:
    - device: N150
      max_concurrency: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "3072"
        VLLM__MAX_MODEL_LENGTH: "384"
        VLLM__MIN_CONTEXT_LENGTH: "32"
        VLLM__MAX_NUM_SEQS: "8"
        MAX_BATCH_SIZE: "8"
        DEFAULT_THROTTLE_LEVEL: "0"
    - device: N300
      max_concurrency: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "6144"
        VLLM__MAX_MODEL_LENGTH: "384"
        VLLM__MIN_CONTEXT_LENGTH: "32"
        VLLM__MAX_NUM_SEQS: "16"
        MAX_BATCH_SIZE: "16"
        DEFAULT_THROTTLE_LEVEL: "0"
    - device: T3K
      max_concurrency: 4
      max_context: 65536  # 64 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "6144"
        VLLM__MAX_MODEL_LENGTH: "384"
        VLLM__MIN_CONTEXT_LENGTH: "32"
        VLLM__MAX_NUM_SEQS: "16"
        MAX_BATCH_SIZE: "16"
        DEFAULT_THROTTLE_LEVEL: "0"
    - device: GALAXY
      max_concurrency: 32
      max_context: 65536  # 64 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "3072"
        VLLM__MAX_MODEL_LENGTH: "384"
        VLLM__MIN_CONTEXT_LENGTH: "32"
        VLLM__MAX_NUM_SEQS: "8"
        MAX_BATCH_SIZE: "8"
        DEFAULT_THROTTLE_LEVEL: "0"
        # Disable Inspector RPC to prevent port conflicts with 32 concurrent workers
        # Each worker would otherwise try to bind to the same port (50051)
        TT_METAL_INSPECTOR_RPC: "0"

- weights:
    - BAAI/bge-m3
  impl: tt_vllm_plugin
  min_disk_gb: 15
  min_ram_gb: 6
  model_type: EMBEDDING
  inference_engine: MEDIA
  device_model_specs:
    - device: N150
      max_concurrency: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "262144"
        VLLM__MAX_MODEL_LENGTH: "8192"
        VLLM__MIN_CONTEXT_LENGTH: "32"
        VLLM__MAX_NUM_SEQS: "32"
        MAX_BATCH_SIZE: "32"
        DEFAULT_THROTTLE_LEVEL: "0"
    - device: N300
      max_concurrency: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "262144"
        VLLM__MAX_MODEL_LENGTH: "8192"
        VLLM__MIN_CONTEXT_LENGTH: "32"
        VLLM__MAX_NUM_SEQS: "32"
        MAX_BATCH_SIZE: "32"
        DEFAULT_THROTTLE_LEVEL: "0"
    - device: T3K
      max_concurrency: 4
      max_context: 65536  # 64 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "262144"
        VLLM__MAX_MODEL_LENGTH: "8192"
        VLLM__MIN_CONTEXT_LENGTH: "32"
        VLLM__MAX_NUM_SEQS: "32"
        MAX_BATCH_SIZE: "32"
        DEFAULT_THROTTLE_LEVEL: "0"
    - device: GALAXY
      max_concurrency: 32
      max_context: 65536  # 64 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "262144"
        VLLM__MAX_MODEL_LENGTH: "8192"
        VLLM__MIN_CONTEXT_LENGTH: "32"
        VLLM__MAX_NUM_SEQS: "32"
        MAX_BATCH_SIZE: "32"
        DEFAULT_THROTTLE_LEVEL: "0"
        # Disable Inspector RPC to prevent port conflicts with 32 concurrent workers
        TT_METAL_INSPECTOR_RPC: "0"

- weights:
    - Qwen/Qwen3-Embedding-8B
  impl: tt_transformers
  min_disk_gb: 15
  min_ram_gb: 6
  model_type: EMBEDDING
  inference_engine: MEDIA
  device_model_specs:
    - device: N150
      max_concurrency: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "1024"
        VLLM__MAX_MODEL_LENGTH: "1024"
        VLLM__MIN_CONTEXT_LENGTH: "32"
        VLLM__MAX_NUM_SEQS: "1"
    - device: N300
      max_concurrency: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "8192"
        VLLM__MAX_MODEL_LENGTH: "4096"
        VLLM__MIN_CONTEXT_LENGTH: "32"
        VLLM__MAX_NUM_SEQS: "2"
    - device: T3K
      max_concurrency: 4
      max_context: 65536  # 64 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "8192"
        VLLM__MAX_MODEL_LENGTH: "4096"
        VLLM__MIN_CONTEXT_LENGTH: "32"
        VLLM__MAX_NUM_SEQS: "2"
    - device: GALAXY
      max_concurrency: 32
      max_context: 65536  # 64 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "1024"
        VLLM__MAX_MODEL_LENGTH: "1024"
        VLLM__MIN_CONTEXT_LENGTH: "32"
        VLLM__MAX_NUM_SEQS: "1"

- weights:
    - Qwen/Qwen3-Embedding-4B
  impl: forge_vllm_plugin
  min_disk_gb: 15
  min_ram_gb: 6
  model_type: EMBEDDING
  inference_engine: FORGE
  device_model_specs:
    - device: N150
      max_concurrency: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "1024"
        VLLM__MAX_MODEL_LENGTH: "1024"
        VLLM__MIN_CONTEXT_LENGTH: "32"
        VLLM__MAX_NUM_SEQS: "1"
        MAX_BATCH_SIZE: "1"
        DEFAULT_THROTTLE_LEVEL: "0"
    - device: N300
      max_concurrency: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "1024"
        VLLM__MAX_MODEL_LENGTH: "1024"
        VLLM__MIN_CONTEXT_LENGTH: "32"
        VLLM__MAX_NUM_SEQS: "1"
        MAX_BATCH_SIZE: "1"
        DEFAULT_THROTTLE_LEVEL: "0"
    - device: T3K
      max_concurrency: 4
      max_context: 65536  # 64 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "1024"
        VLLM__MAX_MODEL_LENGTH: "1024"
        VLLM__MIN_CONTEXT_LENGTH: "32"
        VLLM__MAX_NUM_SEQS: "1"
        MAX_BATCH_SIZE: "1"
        DEFAULT_THROTTLE_LEVEL: "0"
    - device: GALAXY
      max_concurrency: 32
      max_context: 65536  # 64 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "1024"
        VLLM__MAX_MODEL_LENGTH: "1024"
        VLLM__MIN_CONTEXT_LENGTH: "32"
        VLLM__MAX_NUM_SEQS: "1"
        MAX_BATCH_SIZE: "1"
        DEFAULT_THROTTLE_LEVEL: "0"
    - device: BLACKHOLE_GALAXY
      # SPMD data parallel: ONE worker owns all 32 chips. Converted from the
      # per-chip layout because pinning a single chip aborts in device init on
      # Blackhole (tt-xla#5521); SPMD is the only forge multi-chip layout that
      # works today. Validated at 4 chips on QB2 -- full release green.
      max_concurrency: 256   # GLOBAL batch, split across 32 replicas = 8/chip
      # One worker under SPMD, so "full board" is 1 ready worker, not
      # max_concurrency. Without this the benchmark's health gate waits forever.
      expected_ready_workers: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "32768"   # max_num_seqs * max_model_length
        VLLM__MAX_MODEL_LENGTH: "128"
        VLLM__MIN_CONTEXT_LENGTH: "32"
        VLLM__MAX_NUM_SEQS: "256"
        MAX_BATCH_SIZE: "256"
        ENABLE_DATA_PARALLEL: "true"
        # >1 max_num_seqs auto-enables the dynamic batcher, which dispatches via
        # _run_async -- not implemented by the embedding runner.
        USE_DYNAMIC_BATCHER: "false"
        DEFAULT_THROTTLE_LEVEL: "0"
        TT_METAL_INSPECTOR_RPC: "0"

    # QB2 (2x P300 = 4 Blackhole chips): SPMD data-parallel across all four in
    # ONE worker. Scale-down proxy for the 32-chip galaxy that also avoids
    # tt-xla#5521, where pinning a single chip aborts in device init.
    # Deliberately NOT registered in test_module/test_suites/embedding.json, so
    # a release here runs evals + benchmarks with no spec tests.
    - device: P300X2
      max_concurrency: 32   # global batch, split across the 4 DP replicas
      # One worker owns all four chips under SPMD, so "full board" is 1 ready
      # worker, not max_concurrency. Without this the benchmark's FULL_BOARD
      # health gate waits for 32 workers that never appear.
      expected_ready_workers: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "4096"   # max_num_seqs * max_model_length
        VLLM__MAX_MODEL_LENGTH: "128"
        VLLM__MIN_CONTEXT_LENGTH: "32"
        VLLM__MAX_NUM_SEQS: "32"
        MAX_BATCH_SIZE: "32"
        ENABLE_DATA_PARALLEL: "true"
        # Required: >1 max_num_seqs auto-enables the dynamic batcher, which
        # dispatches via _run_async -- not implemented by this runner.
        USE_DYNAMIC_BATCHER: "false"
        DEFAULT_THROTTLE_LEVEL: "0"
        TT_METAL_INSPECTOR_RPC: "0"

- weights:
    - Qwen/Qwen3-Embedding-0.6B
  impl: forge_vllm_plugin
  min_disk_gb: 15
  min_ram_gb: 6
  model_type: EMBEDDING
  inference_engine: FORGE
  device_model_specs:
    - device: BLACKHOLE_GALAXY
      # SPMD data parallel: ONE worker owns all 32 chips. Converted from the
      # per-chip layout because pinning a single chip aborts in device init on
      # Blackhole (tt-xla#5521); SPMD is the only forge multi-chip layout that
      # works today. Validated at 4 chips on QB2 -- full release green.
      max_concurrency: 256   # GLOBAL batch, split across 32 replicas = 8/chip
      # One worker under SPMD, so "full board" is 1 ready worker, not
      # max_concurrency. Without this the benchmark's health gate waits forever.
      expected_ready_workers: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "32768"   # max_num_seqs * max_model_length
        VLLM__MAX_MODEL_LENGTH: "128"
        VLLM__MIN_CONTEXT_LENGTH: "32"
        VLLM__MAX_NUM_SEQS: "256"
        MAX_BATCH_SIZE: "256"
        ENABLE_DATA_PARALLEL: "true"
        # >1 max_num_seqs auto-enables the dynamic batcher, which dispatches via
        # _run_async -- not implemented by the embedding runner.
        USE_DYNAMIC_BATCHER: "false"
        DEFAULT_THROTTLE_LEVEL: "0"
        TT_METAL_INSPECTOR_RPC: "0"
    # QB2 (2x P300 = 4 Blackhole chips): SPMD data-parallel across all four in
    # ONE worker, rather than the per-chip layout above. Scale-down proxy for the
    # galaxy that also avoids tt-xla#5521, where pinning a single chip aborts.
    # Deliberately NOT registered in test_module/test_suites/embedding.json, so
    # a release here runs evals + benchmarks with no spec tests.
    - device: P300X2
      max_concurrency: 32   # global batch, split across the 4 DP replicas
      # One worker owns all four chips under SPMD, so "full board" is 1 ready
      # worker, not max_concurrency. Without this the benchmark's FULL_BOARD
      # health gate waits for 32 workers that never appear.
      expected_ready_workers: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "4096"   # max_num_seqs * max_model_length
        VLLM__MAX_MODEL_LENGTH: "128"
        VLLM__MIN_CONTEXT_LENGTH: "32"
        VLLM__MAX_NUM_SEQS: "32"
        MAX_BATCH_SIZE: "32"
        ENABLE_DATA_PARALLEL: "true"
        # Required: >1 max_num_seqs auto-enables the dynamic batcher, which
        # dispatches via _run_async -- not implemented by this runner.
        USE_DYNAMIC_BATCHER: "false"
        DEFAULT_THROTTLE_LEVEL: "0"
        TT_METAL_INSPECTOR_RPC: "0"

- weights:
    - BAAI/bge-m3
  impl: forge_vllm_plugin
  min_disk_gb: 15
  min_ram_gb: 6
  model_type: EMBEDDING
  inference_engine: FORGE
  device_model_specs:
    - device: BLACKHOLE_GALAXY
      # SPMD data parallel: ONE worker owns all 32 chips. Converted from the
      # per-chip layout because pinning a single chip aborts in device init on
      # Blackhole (tt-xla#5521); SPMD is the only forge multi-chip layout that
      # works today. Validated at 4 chips on QB2 -- full release green.
      max_concurrency: 256   # GLOBAL batch, split across 32 replicas = 8/chip
      # One worker under SPMD, so "full board" is 1 ready worker, not
      # max_concurrency. Without this the benchmark's health gate waits forever.
      expected_ready_workers: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "131072"   # max_num_seqs * max_model_length
        VLLM__MAX_MODEL_LENGTH: "512"
        VLLM__MIN_CONTEXT_LENGTH: "32"
        VLLM__MAX_NUM_SEQS: "256"
        MAX_BATCH_SIZE: "256"
        ENABLE_DATA_PARALLEL: "true"
        # >1 max_num_seqs auto-enables the dynamic batcher, which dispatches via
        # _run_async -- not implemented by the embedding runner.
        USE_DYNAMIC_BATCHER: "false"
        DEFAULT_THROTTLE_LEVEL: "0"
        TT_METAL_INSPECTOR_RPC: "0"
    # QB2 (2x P300 = 4 Blackhole chips): SPMD data-parallel across all four in
    # ONE worker. Scale-down proxy for the 32-chip galaxy that also avoids
    # tt-xla#5521, where pinning a single chip aborts in device init.
    # Deliberately NOT registered in test_module/test_suites/embedding.json, so
    # a release here runs evals + benchmarks with no spec tests.
    - device: P300X2
      max_concurrency: 32   # global batch, split across the 4 DP replicas
      # One worker owns all four chips under SPMD, so "full board" is 1 ready
      # worker, not max_concurrency. Without this the benchmark's FULL_BOARD
      # health gate waits for 32 workers that never appear.
      expected_ready_workers: 1
      max_context: 65536  # 64 * 1024
      default_impl: true
      env_vars:
        VLLM__MAX_NUM_BATCHED_TOKENS: "4096"   # max_num_seqs * max_model_length
        VLLM__MAX_MODEL_LENGTH: "128"
        VLLM__MIN_CONTEXT_LENGTH: "32"
        VLLM__MAX_NUM_SEQS: "32"
        MAX_BATCH_SIZE: "32"
        ENABLE_DATA_PARALLEL: "true"
        # Required: >1 max_num_seqs auto-enables the dynamic batcher, which
        # dispatches via _run_async -- not implemented by this runner.
        USE_DYNAMIC_BATCHER: "false"
        DEFAULT_THROTTLE_LEVEL: "0"
        TT_METAL_INSPECTOR_RPC: "0"
