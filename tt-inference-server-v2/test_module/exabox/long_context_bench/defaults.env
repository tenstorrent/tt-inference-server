# SPDX-FileCopyrightText: (c) 2025 Tenstorrent AI ULC
# SPDX-License-Identifier: Apache-2.0
#
# Defaults for the `long_context_bench` test variant. Mirrors the
# longContextBench: block of the old chart's values.yaml.

: "${MODEL:=deepseek-ai/DeepSeek-R1-0528}"
: "${INPUT_LEN:=128}"
: "${OUTPUT_LEN:=128000}"
: "${NUM_PROMPTS:=5}"
: "${MAX_CONCURRENCY:=5}"
: "${REQUEST_TIMEOUT_SEC:=1800}"
