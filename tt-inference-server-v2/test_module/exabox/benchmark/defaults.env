# SPDX-FileCopyrightText: (c) 2025 Tenstorrent AI ULC
# SPDX-License-Identifier: Apache-2.0
#
# Defaults for the `benchmark` test variant. The actual sweep lives in
# tt-inference-server (run_benchmarks.sh); it reads no env vars besides
# OPENAI_API_BASE/OPENAI_API_KEY, so this file is intentionally minimal.

: "${NUM_PROMPTS:=100}"
