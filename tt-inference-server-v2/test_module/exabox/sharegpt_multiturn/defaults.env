# SPDX-FileCopyrightText: (c) 2025 Tenstorrent AI ULC
# SPDX-License-Identifier: Apache-2.0
#
# Defaults for the `sharegpt_multiturn` test variant. Mirrors the
# sharegptMultiturn: block of the old chart's values.yaml.

: "${MODEL:=deepseek-ai/DeepSeek-R1-0528}"
: "${CONCURRENCY:=64}"
: "${DURATION:=3600}"
: "${MIN_TURNS:=4}"
: "${MAX_TURNS:=20}"
: "${MAX_OUTPUT_TOKENS:=512}"
