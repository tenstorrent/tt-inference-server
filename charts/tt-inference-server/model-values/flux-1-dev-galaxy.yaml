model: FLUX.1-dev
device: galaxy
serverType: media
image:
  repository: ghcr.io/tenstorrent/tt-media-inference-server
  tag: 0.10.0-555f240
resources:
  limits:
    memory: 32Gi
  requests:
    memory: 6Gi
probes:
  liveness:
    initialDelaySeconds: 600
    periodSeconds: 30
  readiness:
    initialDelaySeconds: 600
    periodSeconds: 10
