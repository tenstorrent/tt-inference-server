# model_matrix.toml — Runtime dispatch config
# Single source of truth for model architecture configs and kernel assignments.
# Resolved once at load_model() time by KernelDispatcher.
#
# status field:
#   "validated"  — tested end-to-end on hardware, PCC verified. Served without warnings.
#   "community"  — architecture known but correctness unverified. Requires --unsafe to load.
#
# Dispatcher validates at load:
#   1. schema_version matches DISPATCHER_SCHEMA_VERSION
#   2. n_heads % n_kv_heads == 0 for every entry
#   3. Every validated entry has n_heads >= 1
#
# Do not edit ttl_kernels/configs/ — that is the legacy POC reference only.

schema_version = 1

# ---------------------------------------------------------------------------
# Llama 3 family (Meta)
# ---------------------------------------------------------------------------

[[model]]
name              = "meta-llama/Llama-3-8B-Instruct"
arch              = "LlamaForCausalLM"
local_path        = "models/llama3-8b"
hidden_size       = 4096
intermediate_size = 14336
n_layers          = 32
vocab_size        = 128256
n_heads           = 32
n_kv_heads        = 8
head_dim          = 128
max_seq           = 2048
rope_theta        = 500000.0
swa_window        = -1
tie_embeddings    = false
kernels           = ["flash_attn", "rmsnorm"]
min_hw            = "bh_p150_1card"
status            = "validated"
notes             = "Baseline reference model. GQA 8 KV heads."

[[model]]
name              = "deepseek-ai/DeepSeek-R1-Distill-Llama-8B"
arch              = "LlamaForCausalLM"
local_path        = "models/deepseek-ai--DeepSeek-R1-Distill-Llama-8B"
hidden_size       = 4096
intermediate_size = 14336
n_layers          = 32
vocab_size        = 128256
n_heads           = 32
n_kv_heads        = 8
head_dim          = 128
max_seq           = 2048
rope_theta        = 500000.0
swa_window        = -1
tie_embeddings    = false
kernels           = ["flash_attn", "rmsnorm"]
min_hw            = "bh_p150_1card"
status            = "validated"
notes             = "Llama 3 8B fine-tune. Identical kernel config to llama3-8b."

# ---------------------------------------------------------------------------
# Qwen 2.5 family (Alibaba)
# ---------------------------------------------------------------------------

[[model]]
name              = "Qwen/Qwen2.5-7B-Instruct"
arch              = "Qwen2ForCausalLM"
local_path        = "models/qwen2.5-7b"
hidden_size       = 3584
intermediate_size = 18944
n_layers          = 28
vocab_size        = 152064
n_heads           = 28
n_kv_heads        = 4
head_dim          = 128
max_seq           = 2048
rope_theta        = 1000000.0
swa_window        = 131072
tie_embeddings    = false
kernels           = ["flash_attn", "rmsnorm"]
min_hw            = "bh_p150_1card"
status            = "validated"
issue             = 22
notes             = "BUG-MISTRAL-01: degenerate output, tracked in #22. swa_window=131072 is effectively full attention at max_seq=2048."

# ---------------------------------------------------------------------------
# Mistral family (Mistral AI)
# ---------------------------------------------------------------------------

[[model]]
name              = "mistralai/Mistral-7B-v0.3"
arch              = "MistralForCausalLM"
local_path        = "models/mistral-7b"
hidden_size       = 4096
intermediate_size = 14336
n_layers          = 32
vocab_size        = 32768
n_heads           = 32
n_kv_heads        = 8
head_dim          = 128
max_seq           = 2048
rope_theta        = 1000000.0
swa_window        = -1
tie_embeddings    = false
kernels           = ["flash_attn", "rmsnorm"]
min_hw            = "bh_p150_1card"
status            = "validated"
issue             = 22
notes             = "BUG-MISTRAL-01: hidden-state divergence produces degenerate output. v0.3 has no SWA (removed from v0.1) and rope_theta=1e6."

# ---------------------------------------------------------------------------
# Gemma 2 family (Google)
# ---------------------------------------------------------------------------

[[model]]
name              = "google/gemma-2-2b-it"
arch              = "Gemma2ForCausalLM"
local_path        = "models/google-gemma-2-2b-it"
hidden_size       = 2304
intermediate_size = 9216
n_layers          = 26
vocab_size        = 256000
n_heads           = 8
n_kv_heads        = 4
head_dim          = 256
max_seq           = 2048
rope_theta        = 10000.0
swa_window        = 4096
tie_embeddings    = true
kernels           = ["flash_attn", "rmsnorm"]
min_hw            = "bh_p150_1card"
status            = "validated"
issue             = 10
notes             = "L1 budget borderline due to head_dim=256 (8 tiles). CPU fallback active via #10. 2B variant on disk (not 9B)."

# ---------------------------------------------------------------------------
# Phi-3.5 family (Microsoft)
# ---------------------------------------------------------------------------

[[model]]
name              = "microsoft/Phi-3.5-mini-instruct"
arch              = "Phi3ForCausalLM"
local_path        = "models/microsoft-Phi-3.5-mini-instruct"
hidden_size       = 3072
intermediate_size = 8192
n_layers          = 32
vocab_size        = 32064
n_heads           = 32
n_kv_heads        = 32
head_dim          = 96
max_seq           = 2048
rope_theta        = 10000.0
swa_window        = 262144
tie_embeddings    = false
kernels           = ["flash_attn", "rmsnorm"]
min_hw            = "bh_p150_1card"
status            = "validated"
notes             = "MHA (no GQA, n_kv_heads=n_heads=32). swa_window=262144 is effectively full attention at max_seq=2048. head_dim=96 (3 tiles)."

# ---------------------------------------------------------------------------
# NOTE: Falcon-7b absent by design
# Falcon-7b uses N_heads=71 (prime). _grid_shape(71) has only factorization
# (1, 71) but y-axis limit is 10, so it cannot map to the 13×10 Tensix grid
# using 1 core per head. Requires a multi-head-per-core loop kernel or a
# different dispatch strategy. Track in a dedicated issue before claiming
# Falcon-7b support.
# Falcon-rw-1b (N_heads=16, MQA N_kv=1) is a separate model and may be
# supportable — open a port issue before adding it here.
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# GPTNeoX family (EleutherAI)
# ---------------------------------------------------------------------------

[[model]]
name              = "EleutherAI/pythia-2.8b"
arch              = "GPTNeoXForCausalLM"
local_path        = "models/EleutherAI-pythia-2.8b"
hidden_size       = 2560
intermediate_size = 10240
n_layers          = 32
vocab_size        = 50304
n_heads           = 32
n_kv_heads        = 32
head_dim          = 80
max_seq           = 2048
rope_theta        = 10000.0
swa_window        = -1
tie_embeddings    = false
kernels           = ["flash_attn", "rmsnorm"]
min_hw            = "bh_p150_1card"
status            = "community"
issue             = 11
notes             = "Parallel residual handled. Known limitations: LayerNorm applied as RMSNorm (no mean-sub/bias), partial rotary (rotary_pct=0.25) treated as full RoPE, QKV attention bias ignored. head_dim=80 padded to 96 tiles."

# ---------------------------------------------------------------------------
# Hardware target
# ---------------------------------------------------------------------------

[hardware]
card            = "bh_p150"
tensix_cores    = 120
l1_budget_bytes = 1_440_000
pcie_gen        = 5
device_ids      = [0]
notes           = "Single Blackhole p150. Multi-card support tracked in #17."
