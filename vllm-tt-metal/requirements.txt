# additional inference server and client script requirements
pyjwt==2.7.0
requests==2.32.3

# Pin the FastAPI/Starlette serving stack to the last known-good versions.
# vLLM does not upper-bound these, so a fresh build pulls whatever is latest on
# PyPI. As of 2026-06, fastapi 0.137.x / starlette 1.3.x break
# prometheus-fastapi-instrumentator 8.0.0 (mounted sub-routers surface as
# `_IncludedRouter` objects without a `.path`, so every request 500s with
# `AttributeError: '_IncludedRouter' object has no attribute 'path'`).
# 0.136.3 / 1.2.1 are the versions the working 9970093 image shipped.
# This requirements file installs last in the Dockerfile, so these win over
# whatever vLLM pulled.
fastapi==0.136.3
starlette==1.2.1
